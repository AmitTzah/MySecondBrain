<UserControl x:Class="MySecondBrain.UI.Views.GlobalArtifactsBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ContentBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Search/Filter Bar -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📄 Artifacts" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource ContentForeground}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>

                <Border Background="{DynamicResource InputBackground}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="4" Padding="8,4" Width="200">
                    <TextBlock Text="🔍 Search artifacts..."
                               FontSize="12" Foreground="{DynamicResource SubtleBrush}"/>
                </Border>

                <Border Background="{DynamicResource InputBackground}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="4" Padding="8,4" Margin="8,0,0,0">
                    <TextBlock Text="All Types ▾" FontSize="12"
                               Foreground="{DynamicResource SubtleBrush}"/>
                </Border>

                <Border Background="{DynamicResource InputBackground}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="4" Padding="8,4" Margin="8,0,0,0">
                    <TextBlock Text="All Chats ▾" FontSize="12"
                               Foreground="{DynamicResource SubtleBrush}"/>
                </Border>

                <Border Background="{DynamicResource InputBackground}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="4" Padding="8,4" Margin="8,0,0,0">
                    <TextBlock Text="Newest ▾" FontSize="12"
                               Foreground="{DynamicResource SubtleBrush}"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Row 1: Artifacts Table -->
        <Border Grid.Row="1" Background="{DynamicResource ContentBackground}">
            <TextBlock Text="No artifacts yet"
                       Foreground="{DynamicResource SubtleBrush}"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
