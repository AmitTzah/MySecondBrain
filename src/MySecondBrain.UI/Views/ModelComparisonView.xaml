<UserControl x:Class="MySecondBrain.UI.Views.ModelComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ApplicationBackgroundBrush}"
             AutomationProperties.AutomationId="ModelComparisonView">
    <Grid AutomationProperties.AutomationId="ModelComparisonView">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"
                Padding="12,8">
            <TextBlock Text="⚖️ Model Comparison" FontSize="14" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Border>

        <!-- Row 1: Setup Phase (Persona checklist + prompt input) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Persona Checklist -->
            <Border Grid.Column="0" Background="{DynamicResource ApplicationBackgroundBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0"
                    Padding="12">
                <StackPanel>
                    <TextBlock Text="Select Models" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               Margin="0,0,0,8"/>
                    <CheckBox Content="GPT-4o" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12" Margin="0,0,0,4"/>
                    <CheckBox Content="Claude 3.5 Sonnet" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12" Margin="0,0,0,4"/>
                    <CheckBox Content="Gemini 2.0 Flash" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12" Margin="0,0,0,4"/>
                    <CheckBox Content="DeepSeek V3" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12" Margin="0,0,0,4"/>
                    <CheckBox Content="MiMo" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12" Margin="0,0,0,4"/>
                    <CheckBox Content="Mistral Large" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Right: Prompt Input + Results Placeholder -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Prompt Input -->
                <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"
                        Padding="16,12">
                    <StackPanel>
                        <TextBlock Text="Enter your prompt to compare model responses:"
                                   FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Margin="0,0,0,8"/>
                        <Border Background="{DynamicResource ApplicationBackgroundBrush}"
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                                CornerRadius="6" Padding="12,8" Height="80">
                            <TextBlock Text="Type a prompt..."
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       FontSize="13" VerticalAlignment="Top"/>
                        </Border>
                        <Button Content="Run Comparison" HorizontalAlignment="Right"
                                Background="{DynamicResource AccentFillColorDefaultBrush}"
                                Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                BorderThickness="0"
                                Padding="16,6" FontSize="12" FontWeight="SemiBold"
                                Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Results Phase: Side-by-Side Panels -->
                <UniformGrid Grid.Row="1" Columns="2">
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                            Margin="8" CornerRadius="6" Padding="12">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Model A" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Select models and run comparison"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Margin="0,4,0,0" TextWrapping="Wrap"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                            Margin="8" CornerRadius="6" Padding="12">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="Model B" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Select models and run comparison"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Margin="0,4,0,0" TextWrapping="Wrap"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
