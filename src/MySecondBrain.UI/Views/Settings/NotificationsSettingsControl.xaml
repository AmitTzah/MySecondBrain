<UserControl x:Class="MySecondBrain.UI.Views.Settings.NotificationsSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="🔔 Notifications" FontSize="16" FontWeight="Bold"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="16,16,16,4"/>
        <TextBlock Grid.Row="1" Text="Configure notification preferences for chat completions, streaming, and cross-tab alerts."
                   FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" Margin="16,0,16,12"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="16,0,16,8">
            <Border BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6"
                    Background="{DynamicResource ControlFillColorDefaultBrush}" MaxWidth="500" HorizontalAlignment="Left">
                <StackPanel Margin="14,12,14,12">
                    <CheckBox IsChecked="{Binding SoundOnCompletion}" Content="Play Sound on Completion" FontSize="11"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,8"
                              ToolTip="Play a notification sound when an AI response finishes generating."/>
                    <CheckBox IsChecked="{Binding DisableStreaming}" Content="Disable Streaming" FontSize="11"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,8"
                              ToolTip="Wait for the entire response before displaying it, instead of showing tokens as they are generated."/>
                    <CheckBox IsChecked="{Binding CrossTabCompletionAlert}" Content="Cross-Tab Completion Alert" FontSize="11"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,0"
                              ToolTip="Show an alert when a chat completes in another tab or window."/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
