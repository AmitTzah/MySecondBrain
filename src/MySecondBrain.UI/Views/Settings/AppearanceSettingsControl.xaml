<UserControl x:Class="MySecondBrain.UI.Views.Settings.AppearanceSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:MySecondBrain.UI.Converters">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MySecondBrain.UI;component/Themes/SettingsStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:EnumMatchConverter x:Key="EnumMatchConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="🎨 Appearance" FontSize="16" FontWeight="Bold"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="16,16,16,4"/>
        <TextBlock Grid.Row="1" Text="Customize the chat message layout. WPF-UI Fluent styles control the overall application theme (Light mode only)."
                   FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" Margin="16,0,16,12"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="16,0,16,8">
            <StackPanel>
                <Border BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6"
                        Background="{DynamicResource ControlFillColorDefaultBrush}" Margin="0,0,0,12">
                    <StackPanel Margin="14,12,14,12">
                        <TextBlock Text="Chat Theme" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="Choose the layout style for chat messages."
                                   FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap" Margin="0,0,0,10"/>
                        <ComboBox ItemsSource="{Binding ChatThemeOptions}" SelectedItem="{Binding ChatTheme}"
                                   FontSize="11" Width="200" HorizontalAlignment="Left"
                                   AutomationProperties.AutomationId="AppearanceChatThemeCombo"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
