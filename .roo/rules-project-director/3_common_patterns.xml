<common_patterns>
  <overview>
    Reusable prompt templates and construction patterns that the Project Director uses when delegating to subtask modes. These are the canonical blueprints — use them verbatim or adapt minimally for context.
  </overview>

  <vision_templates>
    <!-- NOTE: Bracket-wrapped text like [What is this app?] are PROMPT PLACEHOLDERS for the Vision Hasher to fill. They are NOT incomplete sections (do not confuse with [TODO]). -->
    <name>Default Vision Templates for Vision Hasher</name>
    <description>
      When the PD needs to provide vision templates to the Vision Hasher (Phase 1, Step 3), write these
      template files directly to agent-workspace/project-director/vision/. Each template contains
      section headers and placeholder prompts that the Vision Hasher fills during the guided interview.

      Templates ARE the vision docs once filled — no separate templates/ directory. The Vision Hasher
      fills them in-place and generates additional per-artifact files (screens/*.md + .html, data/*.md,
      flows/*.md, features/*.md) as artifacts are discovered during the interview.

      The PD creates atomic template files. The VH creates per-artifact subdirectory files during
      the interview (Phase 2 screens, Phase 3 flows, and per-feature specs during Phase 1).
    </description>

    <templates>
      <!-- ATOMIC FILES: PD creates these directly in vision/ -->
      <template name="app-overview.md">
        <location>vision/app-overview.md</location>
        <content><![CDATA[
# App Overview

## Core Purpose
[What is this app? One sentence. What problem does it solve? Who is it for?]

## Elevator Pitch
[Describe the app to a stranger in 3 sentences. They should understand exactly what it does.]

## Key Differentiators
[What makes this app different from existing solutions? List 3-5 unique aspects.]

## Platform
[Web app? Mobile app? Desktop app? CLI tool? Multiple platforms?]

## Success Metrics
[How will we know this app is successful? User count? Revenue? Task completion rate?]
]]></content>
      </template>

      <template name="personas.md">
        <location>vision/personas.md</location>
        <content><![CDATA[
# User Personas

## Primary Persona
[Who is the main user? What is their job/role? What are their goals? What frustrates them about current solutions?]

## Secondary Personas
[Other user types. For each: role, goals, how they differ from the primary persona.]

## Non-Users
[Who explicitly should NOT use this app? Why?]
]]></content>
      </template>

      <template name="feature-inventory.md">
        <location>vision/feature-inventory.md</location>
        <description>
          This is an INDEX file. Each feature gets a brief entry with name, user accomplishment,
          and priority tier. Detailed behavioral specs go in features/[feature-name].md files
          created by the VH during the interview.
        </description>
        <content><![CDATA[
# Feature Inventory

## Core Features
[Features without which the app has no reason to exist. List every one. For each: name, what the user accomplishes, priority. Detailed spec: features/[feature-name].md]

## Secondary Features
[Features that add significant value but aren't critical for v1.]

## Nice-to-Have Features
[Features that would be nice but can wait indefinitely.]

## Explicitly Out of Scope
[Features we are consciously NOT building. This prevents scope creep.]
]]></content>
      </template>

      <template name="edge-cases.md">
        <location>vision/edge-cases.md</location>
        <content><![CDATA[
# Edge Cases & Error Handling

## Global Edge Cases
[Scenarios that apply across the entire app.]

- **First-Time User Experience:** [What happens when a brand new user opens the app?]
- **Empty States:** [Global approach to empty states — what's the pattern?]
- **Network Errors:** [How does the app behave when offline or on slow connections?]
- **Permission Errors:** [What happens when a user tries to access something they shouldn't?]
- **Data Limits:** [Maximum items, file sizes, input lengths — any hard limits?]

## Feature-Specific Edge Cases
[For each feature from the feature inventory, list edge cases.]

### [Feature Name]
- [Edge case 1]
- [Edge case 2]
]]></content>
      </template>

      <!-- SUBDIRECTORIES: PD creates empty directories; VH fills with per-artifact files -->
      <template name="features/">
        <location>vision/features/</location>
        <type>directory</type>
        <description>Per-feature detailed behavioral specs. Created as empty directory by PD.
          VH creates features/[feature-name].md for each core feature during Phase 1 interview.</description>
      </template>

      <template name="data/">
        <location>vision/data/</location>
        <type>directory</type>
        <description>Per-entity data concepts. Created as empty directory by PD.
          VH creates data/[entity-name].md as entities are discovered during Phase 1 interview.</description>
        <per_file_template><![CDATA[
# [Entity Name]

- **Description:** [What is this thing?]
- **Attributes:** [List every piece of information. For each: name, type (text/number/date/etc.), required/optional, constraints]
- **Lifecycle:** [How is it created? Updated? Deleted? By whom?]
- **Relationships:** [How does it relate to other entities? Belongs to? Has many? References?]
- **UI Visibility:** [Which screens show this data?]
]]></per_file_template>
      </template>

      <template name="screens/">
        <location>vision/screens/</location>
        <type>directory</type>
        <description>Per-screen canonical .md specs + interactive .html mocks. Created as empty
          directory by PD. VH creates screens/[screen-name].md and screens/[screen-name].html
          for each screen during Phase 2 interview.</description>
        <per_file_template_md><![CDATA[
# [Screen Name]

- **Purpose:** [What does the user accomplish on this screen?]
- **Layout:** [Single panel? Split panel? Tabs? Cards? List+detail?]
- **Regions:**
  - [Region 1 name]: [What's here? Every button, input, table, chart.]
  - [Region 2 name]: [What's here?]
- **Data Displayed:** [What information is shown? Where does it come from conceptually?]
- **Actions:** [What can the user do from here? Every button, link, interaction. For each: describe exactly what happens — this becomes the tooltip text in the interactive mock.]
- **Empty State:** [What does the user see on first visit / when there's no data?]
- **Loading State:** [What does the user see while data is loading?]
- **Error State:** [What does the user see when something goes wrong?]
- **Navigation:** [How did the user get here? Where can they go from here? List every navigation target.]
]]></per_file_template_md>
        <note>The VH generates companion screens/[screen-name].html interactive mock files
          using a pre-built CSS component library and wireframe aesthetic. The HTML mocks
          are NOT created by PD — they are generated by VH during Phase 2.</note>
      </template>

      <template name="flows/">
        <location>vision/flows/</location>
        <type>directory</type>
        <description>Per-flow user journey specs. Created as empty directory by PD.
          VH creates flows/[flow-name].md during Phase 3 interview.</description>
        <per_file_template><![CDATA[
# [Flow Name]

- **Persona:** [Which user type?]
- **Goal:** [What are they trying to achieve?]
- **Starting Point:** [Which screen do they start on? What state is the app in?]
- **Happy Path:**
  1. [Step 1 — screen, action, what happens]
  2. [Step 2 — screen, action, what happens]
  ...
  N. [Completion — what do they see?]
- **Alternative Paths:** [Different choices the user might make, and where they lead.]
- **Failure Points:** [What can go wrong at each step? How is it handled?]
- **Edge Cases:** [Unusual situations — first-time user, returning after long absence, etc.]
]]></per_file_template>
      </template>
    </templates>
  </vision_templates>

  <vision_hasher_subtask_prompt>
    <name>Vision Hasher Subtask Prompt</name>
    <description>
      Canonical prompt to use when spawning a Vision Hasher subtask in Phase 1, Step 2.
      The Vision Hasher conducts a guided interview to produce a complete interactive
      simulation of the application's end state.
    </description>
    <template>
      Hash out the complete vision for this application. Vision templates are at agent-workspace/project-director/vision/. Fill every template completely.

      The user's initial vision: [USER'S VISION BRIEF]

      Additional context and constraints: [ANY PD CONSTRAINTS]
      Git automation: [git_automation_enabled from state.json]. If false, skip all git commits — save progress to disk only.

      Read vision/vision-config.json to determine the project type and deliverable format. The PD wrote this file before spawning you. The project type is one of: gui, cli, api, library, game, pipeline.

      Common to all project types:
      - Atomic .md files: app-overview.md, personas.md, feature-inventory.md, edge-cases.md
      - Per-artifact subdirectories (project-type-specific — see below)
      - vision-summary.md: comprehensive index with tables linking every artifact
      - vision/state.json: progress tracker for multi-spawn resume

      Project-type-specific deliverable formats:
      - GUI (web/desktop/mobile): screens/ directory with per-screen .md spec + interactive .html mock (wireframe aesthetic). flows/ directory for user journeys.
      - CLI (terminal tool): commands/ directory with per-command .md spec + terminal session transcript. workflows/ directory for command sequences.
      - API (backend service): endpoints/ directory with per-endpoint .md spec + OpenAPI YAML contract. integration_flows/ directory for multi-endpoint sequences.
      - Library (SDK/package): public-api/ directory with per-module .md spec + runnable usage example. usage_flows/ directory for developer integration journeys.
      - Game: screens/ directory (UI screens with .md + .html mocks) + mechanics/ directory (game systems design docs). gameplay_flows/ directory for player journeys.
      - Pipeline (ETL/data): pipeline-stages/ directory with per-stage .md spec + Mermaid data flow diagram. data_flows/ directory for end-to-end data journeys.

      Your job (phase names adapt to project type — read vision/vision-config.json for the exact phases array):
      1. Phase 0 — State Verification: Check for existing vision/state.json. If found, cross-reference with disk and offer resume. If not found, initialize fresh state. Read vision/vision-config.json for project_type.
      2. Phase 1 — Foundation (all types): Fill app-overview.md, personas.md, feature-inventory.md. Create data/[entity].md and features/[feature].md as entities and features are discovered.
      3. Phase 2 — Interface Surface: Generate project-type-specific deliverables per the phases array. GUI: screen interviews → screens/[name].md + .html mocks. CLI: command interviews → commands/[name].md + .session.md transcripts. API: endpoint interviews → endpoints/[name].md + .openapi.yaml contracts. Library: module interviews → public-api/[module].md + .example.* files. Game: screen + mechanics interviews → screens/[name].md + .html + mechanics/[system].md. Pipeline: stage interviews → pipeline-stages/[stage].md with Mermaid diagrams.
      4. Phase 3 — Journeys/Flows: Document end-to-end journeys (flows/, workflows/, integration_flows/, usage_flows/, gameplay_flows/, or data_flows/ depending on project type).
      5. Edge Cases Phase: Fill edge-cases.md with global and per-feature edge cases.
      6. Final Phase — Wire-Up/Walkthrough: Cross-reference all files for consistency, regenerate deliverables (HTML placeholders → real links for GUI/game), generate vision-summary.md with comprehensive index tables adapted to project type.
      7. Use attempt_completion to return to me. Include: file manifest, feature count, project-type-appropriate artifact counts, flagged concerns, and path to vision-summary.md as the primary entry point.

      Maintain vision/state.json throughout — update after every confirmed artifact. This enables seamless resume across multiple spawns.

      For GUI and game projects: interactive HTML mocks use a deliberate wireframe aesthetic (dashed borders, grayscale, "NOT FINAL DESIGN" banner). If the user fixates on CSS/visual polish, redirect: "That's UI Designer territory — for now, is the element in the right place with the right behavior?"

      Do NOT make technical decisions (no tech stack, no architecture). Flag any concerns for orchestrator review.
    </template>
  </vision_hasher_subtask_prompt>

  <technology_sourcing_prompt>
    <name>Technology Sourcing Study Prompt (Phase 2 Step 2)</name>
    <description>
      Canonical prompt for the Architect to evaluate technology sourcing decisions for every major component before writing the planning/ directory. This is where "lego piece" sizing happens: for each component, the Architect evaluates whether to build custom, study a reference implementation, use an open-source library, adopt a full platform/framework, or call a SaaS.
    </description>
    <instructions>
      <item number="1">
        <title>Read All Vision Documents</title>
        Read every file in agent-workspace/project-director/vision/ (except .html wireframes). Identify every major component, integration point, and domain that requires a technology decision.
      </item>
      <item number="2">
        <title>For Each Major Component, Evaluate Sourcing Options</title>
        For every major component identified, evaluate the following sourcing categories. Present the tradeoffs for each viable option, then recommend one with justification.

        <sourcing_categories>
          <category name="Custom Build">
            <description>Build from scratch. Maximum control, maximum effort. Best when: the component is the app's core differentiator, no existing solution fits, or the requirements are highly specific.</description>
            <tradeoff>Full control over behavior, no dependency risk, but highest development and maintenance cost.</tradeoff>
          </category>

          <category name="Reference Implementation">
            <description>Study how an existing product implements the feature, extract design patterns, and adapt them to your platform. Medium-high control, medium effort.</description>
            <analysis_methods>
              <method platform="web">Analyze the JavaScript bundles, network traffic, and DOM structure of the reference product. Identify design patterns (not copy code). Document: component architecture, data flow, state management approach, rendering strategy.</method>
              <method platform="desktop">Analyze behavior patterns via accessibility trees or UI automation. Look for open-source alternatives that implement similar features. Document: UI composition pattern, event handling, data binding approach.</method>
              <method platform="mobile">Analyze via accessibility inspectors or open-source alternatives. Document: screen composition, navigation patterns, state management.</method>
              <method platform="cli">Analyze command structure, output formatting, and interaction patterns of reference CLIs.</method>
            </analysis_methods>
            <output>For each reference implementation analyzed: save design pattern documentation to agent-workspace/external-docs/ref-[product]-[feature].md. Describe the extracted patterns, how they map to your target platform, and what adaptations are needed.</output>
            <example>For a ChatGPT clone as a native Windows desktop app: analyze how claude.ai implements "artifacts" by studying the browser-side code. Extract the content block registry pattern, the incremental rendering strategy, the sandboxed execution model. Document these patterns in external-docs/ref-claude-artifacts.md. Adapt the patterns to WPF/WinUI: content blocks become UserControls, sandboxed execution becomes AppDomain isolation or WebView2.</example>
          </category>

          <category name="Open-Source Library">
            <description>Integrate via API. Medium control (constrained by library API), lower effort. Best when: a mature, well-maintained library exists that matches requirements closely.</description>
            <evaluation_criteria>
              <criterion>Maintenance activity: last commit within 6 months? Active issue resolution?</criterion>
              <criterion>License compatibility: does the license allow commercial use if needed?</criterion>
              <criterion>API surface: does the library's API match the abstraction you'd design yourself?</criterion>
              <criterion>Bundle/dependency cost: how much does it pull in?</criterion>
              <criterion>Platform compatibility: does it work on the target platform?</criterion>
            </evaluation_criteria>
          </category>

          <category name="Full Platform / Framework">
            <description>Large "lego piece" that provides many capabilities at once (e.g., Firebase, Supabase, Electron, Next.js). Lowest control among self-hosted options, lowest effort. Best when: the platform aligns with most requirements and you're willing to adapt to its patterns.</description>
            <tradeoff>Fastest development, but you inherit the platform's limitations, update cadence, and architectural patterns. Switching costs are very high.</tradeoff>
          </category>

          <category name="SaaS / Cloud Service">
            <description>API call to external service. Lowest control, lowest effort, but ongoing cost and vendor lock-in. Best when: the component is not a core differentiator and a reliable service exists.</description>
            <tradeoff>Zero maintenance, but monthly cost, latency dependency, and vendor risk (price changes, API deprecation, shutdown).</tradeoff>
          </category>
        </sourcing_categories>
      </item>

      <item number="3">
        <title>Write the Technology Sourcing Document</title>
        Write agent-workspace/project-director/tech-sourcing.md. Structure:

        # Technology Sourcing Decisions

        ## Platform
        [Target platform from vision: web/desktop/mobile/CLI. Primary OS, form factor.]

        ## Component-by-Component Analysis
        [For EACH major component/domain:]

        ### [Component Name]
        - **Vision Requirement:** [What the vision says this component must do.]
        - **Sourcing Recommendation:** [custom / reference-impl / open-source / platform / saas]
        - **Recommended Approach:** [Specific technology or reference product name.]
        - **Rationale:** [Why this category and this specific choice?]
        - **Alternatives Considered:** [What else was evaluated and why rejected?]
        - **Tradeoffs:** [What do we gain? What do we lose? Control vs. effort assessment.]
        - **Risk Level:** [Low/Medium/High — with reasoning.]

        ## Reference Implementation Analyses
        [For each reference implementation studied:]
        - **Product:** [Name and version/date analyzed.]
        - **Feature Analyzed:** [Which feature?]
        - **Key Patterns Extracted:** [Summary. Full analysis in external-docs/ref-[product]-[feature].md.]
        - **Adaptation Notes:** [How patterns translate to target platform.]

        ## Sourcing Summary
        [Table: Component | Category | Recommendation | Risk Level]
      </item>

      <item number="4">
        <title>Save Reference Analyses to external-docs/</title>
        For each reference implementation analyzed, write a detailed analysis to agent-workspace/external-docs/ref-[product]-[feature].md. Include: architecture overview, key design patterns, data flow, component structure, and platform adaptation notes. These are permanent reference materials for the feature developers.
      </item>

      <item number="5">
        <title>Completion</title>
        When tech-sourcing.md is written and all reference analyses are saved, use attempt_completion immediately. Do not ask for user approval — the PD handles the review gate. Do NOT write the planning/ directory or roadmap.
      </item>
    </instructions>
  </technology_sourcing_prompt>

  <planning_directory_prompts>
    <name>Architect Prompts for Planning/ Directory (Phase 2 Step 3)</name>
    <description>
      The PD spawns the Architect 2-3 times to create the planning/ directory files. The Architect
      reads vision docs and tech-sourcing.md, then creates focused topic files one at a time or in
      batches. This replaces the old monolithic prd_architecture_prompt.
    </description>
    <instructions>
      <item number="1">
        <title>Read Vision Documents, Technology Sourcing, and Existing Planning Files</title>
        Read every file in agent-workspace/project-director/vision/ (except .html wireframes). Read agent-workspace/project-director/tech-sourcing.md. Read any existing files in agent-workspace/project-director/planning/ (if resuming).
      </item>
      <item number="2">
        <title>Create the Planning/ Directory Files</title>
        Write to agent-workspace/project-director/planning/. Create files in this order, one at a time or in logical batches:

        <file name="architecture.md">
          <description>High-level system design: component diagram (conceptual — describe major components and their relationships), data flow between components, architectural patterns (MVVM, Repository, Provider, etc.), deployment model. Reference the sourcing decisions from tech-sourcing.md for each component.</description>
        </file>

        <file name="tech-stack.md">
          <description>Per-component technology stack with sourcing justifications. For each component: the specific technology chosen, the version (if knowable), why it was chosen (referencing vision requirements and tech-sourcing.md decisions), and any constraints it imposes.</description>
        </file>

        <file name="abstractions.md">
          <description>Every provider interface and contract. For each integration point: interface name, key methods, what it abstracts, and the first concrete implementation (e.g., ILLMProvider → OpenAI implementation). These interfaces are what Wave 1 builds and Wave 3 features extend. Derive from tech-sourcing.md's sourcing decisions — every SaaS/open-source/library choice needs an abstraction.</description>
          <generalization_rules>
            <rule>Every external service integration must have a defined abstraction interface.</rule>
            <rule>Write abstractions as: "Build [abstraction] interface; implement [concrete] as first [provider/implementation]."</rule>
            <example bad="Feature: Integrate OpenAI for task suggestions" good="Build ILLMProvider interface with GenerateAsync(); implement OpenAiProvider as first provider"/>
          </generalization_rules>
        </file>

        <file name="data-model.md">
          <description>Entities and their relationships, derived from vision/data/ files. For each entity: name, attributes, relationships to other entities, and which features consume it. Include an entity-relationship summary (conceptual diagram or list).</description>
        </file>

        <file name="integration-points.md">
          <description>Every external integration point: the service, the abstraction that wraps it (reference abstractions.md), the fallback strategy, and any configuration needed. Include both SaaS integrations and platform-specific integrations (e.g., Windows registry, file system, OS notifications).</description>
        </file>

        <file name="platform-notes.md">
          <description>Platform-specific patterns, constraints, and conventions. For WPF: MVVM pattern, XAML bindings, dependency injection patterns. For web: routing strategy, state management, SSR vs CSR. For CLI: argument parsing, output formatting. For desktop: installer requirements, auto-update mechanism, OS integration points.</description>
        </file>

        <file name="planning-summary.md">
          <description>Index file that ties the planning/ directory together. List each file with a one-sentence summary. Include a "Quick Reference" section: tech stack summary table, key abstractions list, data entities list, external integrations list. This is what the FD Architect reads first to understand the architecture.</description>
        </file>
      </item>
      <item number="3">
        <title>Completion</title>
        When the assigned planning/ files are written, use attempt_completion immediately. Do not ask for approval — the PD handles the review gate. List which files were created in your completion summary.
      </item>
    </instructions>
  </planning_directory_prompts>

  <e2e_authoring_guide_prompt>
    <name>Architect Prompt for E2E Authoring Guide (Phase 2 Step 3, Last Batch)</name>
    <description>
      After tech-stack.md and platform-notes.md exist, the PD spawns the Architect to generate
      planning/e2e-authoring-guide.md — a project-type-adaptive guide that encodes the conventions,
      patterns, and principles for writing reliable, fast, and maintainable end-to-end tests.
      This guide is generated ONCE during planning and updated during Phase 4 drift reconciliation
      as new patterns emerge. The Architect MUST explain the approach in its completion summary
      so the PD can present it for human discussion.
    </description>
    <instructions>
      <item number="1">
        <title>Read the Planning/ Directory and Vision Config</title>
        Read all existing planning/ files — especially tech-stack.md, platform-notes.md, architecture.md, and abstractions.md. Read vision/vision-config.json to determine project_type. Read the vision docs to understand the app's scope and interaction patterns.
      </item>
      <item number="2">
        <title>Determine E2E Framework</title>
        Based on the project type and tech stack, select the appropriate E2E testing framework:
        - GUI/web (React, Next.js, Vue, etc.): Playwright or Cypress
        - GUI/desktop (.NET WPF, WinUI, WinForms, Avalonia): WinAppDriver with FlaUI, or Appium with WinAppDriver
        - GUI/desktop (Electron, Tauri): Playwright (Electron support) or Spectron
        - GUI/mobile (React Native, Flutter): Appium, Maestro, or Detox
        - CLI: custom test harness (bash scripts, expect, or language-native process execution)
        - API: Supertest, REST Client, or HttpClient-based test projects
        - Library/SDK: integration test projects that exercise the public API
        - Game (Unity): Unity Test Framework with PlayMode tests
        - Game (Godot): GUT (Godot Unit Testing) with scene-based tests
        - Game (Unreal): Unreal Automation System
        - Game (custom engine): custom test harness with engine-level state queries
        - Pipeline: stage-isolated test harnesses with data contract verification
        Document the rationale for the framework choice (why it fits this specific project).
      </item>
      <item number="3">
        <title>Design Test Infrastructure</title>
        Cover these infrastructure concerns, adapted to the project type:
        <infrastructure_items>
          <item name="headless_execution" priority="critical">
            PREFER HEADLESS EXECUTION whenever the project type and framework support it. Headless mode eliminates the "loss of control" problem entirely — no visible browser window, no mouse/keyboard takeover, no countdown needed.
            Headless capability by project type:
            - Web (Playwright): YES — `headless: true` in config. Chromium, Firefox, WebKit all support headless. This is the DEFAULT and STRONGLY PREFERRED mode. Only use headed mode for debugging specific visual issues.
            - Web (Cypress): YES — `cypress run` is headless by default. `cypress open` is interactive (headed) for development only.
            - CLI: N/A — already headless by nature (no GUI).
            - API: N/A — already headless by nature (HTTP requests).
            - Library: N/A — already headless by nature (code execution).
            - Pipeline: N/A — already headless by nature (data processing).
            - Desktop GUI (.NET WPF/WinUI/WinForms): NO — FlaUI and WinAppDriver require a real desktop session. Mouse/keyboard automation needs visible windows. Headless is not possible. The hands-off countdown IS required.
            - Desktop GUI (Electron): PARTIAL — Playwright can drive Electron in headless mode, but native OS dialogs (file pickers, notifications) still require a display.
            - Mobile (Appium): PARTIAL — Android emulators can run headless; iOS simulators require a display.
            - Game (Unity): PARTIAL — `-batchmode -nographics` for server builds; PlayMode tests typically need a display for rendering.
            - Game (Godot): PARTIAL — `--headless` for server builds; scene tests may need rendering.
            Document: for this specific project, is headless possible? If yes: make it the default. If partial: document what works headless and what doesn't. If no: explain why and note that the hands-off countdown is mandatory.
          </item>
          <item name="fixture_pattern">How tests share app instances: collection fixtures (one launch for all tests) vs class fixtures (launch per class). Recommend the most efficient approach for the project type. For desktop apps: one launch for all tests via shared fixture. For web: Playwright's built-in browser context isolation.</item>
          <item name="test_database">If the app uses a database: strategy for test isolation — separate test DB, environment variable overrides, transaction rollback, or seeded test data. Document how the app selects the test DB path and how the fixture manages it.</item>
          <item name="hands_off_countdown" condition="headless execution is NOT available for this project type">
            REQUIRED when headless is not possible: the E2E fixture MUST display a countdown window before launching the app, giving the human time to release mouse and keyboard. This applies to desktop GUI apps (.NET WPF/WinUI), headed web tests (debugging only), and games without headless mode. The automation drives real mouse/keyboard — human inputs compete with automation and cause flaky tests. Document:
            - Countdown behavior: 3 → 2 → 1 → "GO!" flash
            - Warning text: "⚠️ HANDS OFF — Release mouse & keyboard"
            - CI detection: skip countdown when CI environment variable is set (CI has no human to interfere)
            - Implementation: separate STA thread before app launch (for desktop)
            This section tells the user exactly what to expect when E2E tests run (sudden mouse/keyboard takeover, countdown window appearance). If headless mode IS available, state explicitly: "Headless mode is used — no countdown needed. Tests run invisibly. You can keep using your machine normally."
          </item>
          <item name="cleanup_strategy">Self-cleaning tests: every test that creates data must delete it within the same test body. No class-level Dispose() with multi-step cleanup. No static cleanup counters. Rationale: cascading failures from stale data.</item>
          <item name="no_dead_time">Maximum timeouts: 3 seconds for UI element discovery. No Thread.Sleep() over 500ms without justification. Prefer polling with short intervals over long sleeps.</item>
        </infrastructure_items>
      </item>
      <item number="4">
        <title>Define Selector and Assertion Strategies</title>
        Adapt to project type:
        <selector_strategies>
          <strategy type="gui_web">
            Priority: data-testid → accessibility role/label → semantic HTML → CSS class (last resort).
            Document a naming convention for test IDs if the project doesn't have one.
          </strategy>
          <strategy type="gui_desktop">
            Priority: AutomationId (x:Name in XAML) → Name (exact) → Name (partial) → ControlType scanning.
            If the project uses XAML, document AutomationId naming conventions:
            - Navigation buttons: Nav{ScreenName}
            - Action buttons: {Action}{Entity}Button
            - Input fields: {Entity}{Field}Input
            - ComboBoxes: {Entity}{Property}Combo
            - Views: {ScreenName}View
          </strategy>
          <strategy type="cli">
            Assertions on: exit codes (0 = success, non-zero with specific codes for error types), stdout patterns (regex or exact match), stderr content, file output existence/content.
            Document the project's exit code conventions.
          </strategy>
          <strategy type="api">
            Assertions on: HTTP status codes, response body structure (JSON schema or key fields), response headers, timing/SLA.
            Document auth token management (how the test obtains and refreshes tokens).
          </strategy>
          <strategy type="game">
            Priority: game object properties (health, position, state) → scene state (loaded, active) → UI element queries (engine-specific) → frame captures (visual verification, last resort).
            Engine-specific: Unity (GameObject.Find + GetComponent), Godot (get_node + property access), Unreal (Automation System commandlets).
            Document input simulation patterns (keyboard, mouse, gamepad) and frame-based waiting (WaitForFrames vs real-time delays).
          </strategy>
          <strategy type="library">
            Assertions on: return values, thrown exceptions, side effects (files created, network calls made), performance benchmarks.
            Document how to exercise the public API surface from test code.
          </strategy>
          <strategy type="pipeline">
            Assertions on: stage inputs/outputs, data schemas at each stage boundary, error handling (malformed input → expected error), throughput/latency.
            Document stage isolation: how to test a single stage without running the full pipeline.
          </strategy>
        </selector_strategies>
      </item>
      <item number="5">
        <title>Define Helper Conventions</title>
        Document the base class and shared helper pattern:
        - All shared helpers go in an abstract test base class
        - Per-class helpers are private methods in the test class
        - No duplicated finder/wait/navigate helpers across classes
        - Naming conventions for helper methods
      </item>
      <item number="6">
        <title>Document Test Organization</title>
        Describe how test files should be organized: naming convention (feature-[sanitized-name].ext), one file per feature, test classes grouped by domain area, test ordering considerations (ordering-dependent tests need explicit guards).
      </item>
      <item number="7">
        <title>Include Quick-Reference Checklist</title>
        Add a checklist section at the end of the guide. When writing a new E2E test, verify:
        - Framework-specific setup requirements
        - Fixture pattern correct (collection vs class)
        - Test database isolated (if applicable)
        - All timeouts ≤ 3 seconds
        - No Thread.Sleep() > 500ms without comment
        - Self-cleaning: create → verify → delete → verify deleted
        - Selector priority followed
        - Shared helpers in base class, per-class helpers private
        - MessageBox/dialog handling documented (for GUI)
        - Run command documented
      </item>
      <item number="8">
        <title>Explain the Approach — Converse with the User</title>
        <priority>critical</priority>
        In your completion summary, explain the E2E approach conversationally so the PD can present it to the user. Cover:
        - Framework choice and why
        - Headless capability: can tests run invisibly in the background? If yes (web, CLI, API): "Tests run headless — you can keep using your machine normally. No windows will pop up." If partial: explain what runs headless and what doesn't. If no (desktop GUI): explain why and note the countdown is mandatory.
        - What the user will see when E2E tests run: if headless → "terminal output only, no visible windows"; if headed → countdown, app launching, mouse/keyboard automation for desktop, browser opening for web
        - How test data is isolated from their real data
        - That tests clean up after themselves
        - Any prerequisites the user needs to install (WinAppDriver, Playwright browsers, Appium server)
        This is NOT a rubber-stamp approval — the user needs to understand and agree to the E2E approach before any test takes control of their machine.
      </item>
      <item number="9">
        <title>Write the File</title>
        Write the complete guide to agent-workspace/project-director/planning/e2e-authoring-guide.md. Use the structure: 1. Headless Execution (preferred when available — explains whether tests run invisibly or take control), 2. Fixture Pattern, 3. Test Database/Infrastructure, 4. Hands-Off Countdown (only when headless is not available), 5. Self-Cleaning Tests, 6. No Dead Time, 7. Constant Visual Movement (GUI) / Active Interaction (other types), 8. Helper Conventions, 9. Selector Strategy, 10. Test Organization, 11. Running the E2E Suite, 12. Quick Reference Checklist. Adapt section titles and content to the project type — a CLI guide won't have "Selector Strategy" (it has "Output Assertion Patterns"), a game won't have "UIA Selectors" (it has "Game State Queries"). The headless section is always first — it sets expectations for whether the user loses control of their machine.
      </item>
      <item number="10">
        <title>Completion</title>
        Use attempt_completion immediately. Do not ask for approval — the PD handles the review gate with the user. Your completion summary MUST include a conversational explanation of the E2E approach (see item 8).
      </item>
    </instructions>
  </e2e_authoring_guide_prompt>

  <wave_map_prompt>
    <name>Architect Prompt for Wave Map (Phase 2 Step 4)</name>
    <description>
      Canonical prompt for the Architect to write a lightweight wave map to roadmap.md. Features are
      grouped by architectural wave with ONLY: feature name, 1-line description, and dependencies.
      NO acceptance criteria — the FD Architect derives those from vision docs.
      This replaces the old feature_specs_prompt and feature_specs_continuation_prompt (which are now deprecated).
    </description>
    <instructions>
      <item number="1">
        <title>Read All Input Materials</title>
        Read every file in agent-workspace/project-director/vision/ (except .html wireframes). Read all files in agent-workspace/project-director/planning/ (especially architecture.md, abstractions.md, data-model.md). Read agent-workspace/project-director/tech-sourcing.md for sourcing decisions.
      </item>
      <item number="2">
        <title>Create the Wave Map</title>
        Write agent-workspace/project-director/roadmap.md. Group features into architectural Waves (NOT thematic milestones).

        <wave name="Wave 1: Foundation">
          <description>Infrastructure that everything else depends on. These features build the core abstractions, data model, and project scaffolding — same for all project types.</description>
          <includes>All data entities (from vision data concepts), core abstraction interfaces (providers from planning/abstractions.md), database migrations, project setup, CI/CD pipeline.</includes>
        </wave>

        <wave name="Wave 2: Skeleton">
          <description>Structural frame that Wave 3 fills in. The specifics depend on project type (determined by vision/vision-config.json):</description>
          <includes>
            - GUI: App shell, routing, auth framework, empty screens
            - CLI: CLI framework, argument parsing, config loading, command stubs
            - API: Server framework, middleware chain, route registration, endpoint stubs
            - Library: Module structure, public API skeleton, build/publish pipeline
            - Game: Engine bootstrap, game loop, input handling, placeholder UI screens
            - Pipeline: Orchestrator setup, stage scaffolding, source/sink connectors
          </includes>
        </wave>

        <wave name="Wave 3: Vertical Slices">
          <description>End-to-end features that ADD new code to pre-built infrastructure — they do NOT modify Wave 1-2 code. Format depends on project type:</description>
          <includes>
            - GUI: End-to-end features (DB→API→UI)
            - CLI: End-to-end command groups (parsing→processing→output)
            - API: End-to-end resource groups (endpoint→validation→persistence)
            - Library: End-to-end module chains (public API→internal logic→error handling)
            - Game: End-to-end gameplay features (input→mechanics→feedback)
            - Pipeline: End-to-end data flows (source→transform→destination)
            Every user-facing feature from vision feature-inventory.md. Order by dependency (e.g., Task CRUD before Task Assignment). Each feature references which Wave 1 abstraction it extends.
          </includes>
        </wave>

        <wave name="Wave 4: Cross-Cutting">
          <description>Features that span across all vertical slices. These enhance the entire app uniformly. Scope depends on project type:</description>
          <includes>
            - GUI: Permissions, search, reporting, i18n, E2E suite
            - CLI: Shell completion, config profiles, logging, plugin system
            - API: Rate limiting, caching, versioning, webhook dispatch
            - Library: Error standardization, logging hooks, plugin architecture
            - Game: Save system, achievements, localization, accessibility
            - Pipeline: Monitoring, alerting, schema evolution, data lineage
          </includes>
        </wave>

          <rule>Format:
            - Feature N: [Name] — [1-line description]. Depends on: [list or none].
              E2E: [2-3 sentence concrete verification of this feature's end-to-end behavior. Describe what happens from the user's (or caller's) perspective. Tailor the format to the project type:

              Web app: "Navigate to /login, enter 'user@test.com' + 'pass', click Sign In → redirected to /dashboard, see 'Welcome, User' heading."
              Desktop GUI: "Launch app, click File → New Project, enter 'MyProject', click Create → main window title bar shows 'MyProject'."
              CLI program: "Run `mycli process --input test.csv --output out.json`, verify exit code 0, stdout contains 'Processed: 42 rows', out.json exists with expected schema."
              API backend: "POST /api/users with `{name:'Test',email:'t@t.com'}` → HTTP 201 + JSON body with `id`. GET /api/users/{id} → 200 + same user object."
              Library/SDK: "Call `Lib.parse(validInput)` → returns ParsedResult with expected fields. Call `Lib.parse(invalidInput)` → throws ParseError with message 'Invalid format at line 3'."
              Mobile app: "Tap 'Add Item' FAB, fill Name field with 'Milk', tap Save → item list shows 'Milk' with checkbox."
              Game: "Player presses W → character moves forward. Player reaches checkpoint → 'Checkpoint Reached' UI appears. Player falls off edge → respawn at last checkpoint."
              Pipeline: "Run pipeline with `sample_input.csv`, verify output data validates against schema, monitoring events fired for each stage, dead-letter queue contains 0 messages for valid input."

              NOT vague: "Test login flow." NOT implementation: "Verify JWT token is in localStorage."]

            If E2E is genuinely not applicable, write: E2E: Not applicable — [project-type-specific reason].
            Valid "Not applicable" examples:
            - Game engine plugin: "Not applicable — engine plugin, tested via integration tests."
            - Data pipeline: "Not applicable — batch processor, verified via integration tests on sample data."
            - Pure infrastructure library: "Not applicable — no user-facing API, tested via unit tests."

            Wave 1 Foundation features (data model, CI/CD, project scaffold) may legitimately have no E2E.
            Wave 2 Skeleton features (app shell, routing, empty screens) may have minimal E2E.
            Wave 3+ features: "Not applicable" requires strong justification. If a Wave 3 feature has no verifiable end-to-end behavior, ask yourself: should this feature be merged into another feature instead of being standalone?
            Wave 4 features: should have E2E for user/caller-facing aspects.
          </rule>
          <rule>Testing and DevOps MUST be explicit features within appropriate waves.</rule>
          <rule>Each feature in Wave 3 must note: "Extends [Wave 1 abstraction]" or "Uses [Wave 1 entity]" so it's clear what pre-built infrastructure supports it.</rule>
          <rule>For features sourced via reference implementation: note "Reference pattern: external-docs/ref-[product]-[feature].md" in the feature description.</rule>
          <rule>DO NOT write implementation acceptance criteria (the FD Architect derives those from vision docs). E2E specs are different: they describe concrete user journeys for the regression test suite, not implementation requirements.</rule>
          <rule>DO NOT number features yet — numbering happens in the assembly step.</rule>
        </format_rules>
      </item>
      <item number="3">
        <title>Completion</title>
        When roadmap.md is written with all features for all waves, use attempt_completion immediately. Do not ask for approval. Do NOT include acceptance criteria. Do NOT number features — the assembly step handles that.
      </item>
    </instructions>
  </wave_map_prompt>

  <assembly_verification_prompt>
    <name>Architect Prompt for Assembly — Numbering & Dependency Check (Phase 2 Step 5)</name>
    <description>
      Simplified assembly prompt: number all features sequentially, verify cross-wave dependencies, ensure forward-flow. No generalization sweep (that's the PD's Step 7 audit).
    </description>
    <instructions>
      <item number="1">
        <title>Read the Complete Wave Map and Planning Directory</title>
        Read agent-workspace/project-director/roadmap.md — all features across all waves should be specified. Also read the planning/ directory files (architecture.md, abstractions.md, data-model.md).
      </item>
      <item number="2">
        <title>Number All Features Sequentially</title>
        Replace temporary labels with sequential numbers: Feature 1, Feature 2, Feature 3, ... across all waves. Ensure numbering is continuous (no gaps, no duplicates).
      </item>
      <item number="3">
        <title>Verify Cross-Wave Dependencies</title>
        Check every feature's dependency list. For each dependency:
        - Does the dependency feature exist in the roadmap?
        - Is the dependency feature numbered BEFORE the dependent feature (forward flow)?
        - Does any Wave 3 feature incorrectly depend on another Wave 3 feature that hasn't been built yet? (Wave 3 features should depend on Wave 1-2 features, not on each other unless explicitly ordered.)
        If any dependency violations are found: fix the dependency list or reorder features as needed.
      </item>
      <item number="4">
        <title>Verify Abstraction References</title>
        Every Wave 3 feature must note which Wave 1 abstraction(s) from planning/abstractions.md it extends. Verify these references are correct: does the abstraction actually exist in the planning/ directory? If a Wave 3 feature needs an abstraction that isn't in the planning/ directory, flag it for the PD.
      </item>
      <item number="5">
        <title>Completion</title>
        When assembly is complete, use attempt_completion with summary: total features, features per wave, any unresolved dependency or abstraction issues flagged.
      </item>
    </instructions>
  </assembly_verification_prompt>

  <feature_developer_prompt_construction>
    <overview>
      Every time you spawn a feature-developer subtask, the prompt must be comprehensive and self-contained. The feature-developer operates independently and needs full context to succeed.
    </overview>

    <required_elements>
      <element name="project_vision">
        <description>
          Copy the core vision, user personas, and success metrics from agent-workspace/project-director/vision/app-overview.md and vision/personas.md. Also include the tech stack summary from planning/tech-stack.md. This gives the feature-developer the "North Star" context for their work.
        </description>
      </element>
      <element name="feature_specification">
        <description>
          Copy the exact feature line from agent-workspace/project-director/roadmap.md (name, 1-line description, dependencies, wave). Include the wave name so the FD understands the architectural context.

          CRITICAL: The roadmap contains ONLY the feature name, 1-line description, and dependencies — NO acceptance criteria. Include this instruction: "Read the vision docs for this feature. Derive acceptance criteria from the vision. Create an implementation plan using the architecture and abstractions from planning/."

          PRE-RESOLVE VISION FILES (do NOT just dump directory paths):
          Before constructing the FD spawn prompt, read vision-summary.md and locate the target feature in the Feature Index table (Feature | Tier | Screens Used | Data Entities). Extract the specific screens and data entities listed for this feature. Then include these EXACT file paths in the FD spawn prompt:
          1. features/[feature-name].md — the detailed behavioral spec for this specific feature
          2. screens/[screen-a].md, screens/[screen-b].md, ... — the canonical AI-mode specs for each screen this feature appears on (from the Feature Index "Screens Used" column)
          3. data/[entity-x].md, data/[entity-y].md, ... — the data entity specs this feature touches (from the Feature Index "Data Entities" column)
          4. vision-summary.md and feature-inventory.md — for broader context and cross-reference navigation
          5. Also note the corresponding HTML mock files: screens/[screen-a].html, screens/[screen-b].html — these are interactive mocks for human simulation; the FD Architect may open them in a browser for visual understanding but derives acceptance criteria from the .md specs

          ⚠️ PROJECT-TYPE NOTE: The artifact paths shown above (screens/*.md, screens/*.html, data/*.md) are the GUI default. The PD workflow Phase 3 Step 2 overrides these paths based on project_type from vision/vision-config.json. For non-GUI project types, artifact paths differ:
          - CLI: commands/*.md, transcripts/*.txt (no HTML mocks)
          - API: endpoints/*.md, contracts/*.yaml (OpenAPI specs)
          - Library: api-surface/*.md, examples/*.md (runnable usage examples)
          - Game: screens/*.md, screens/*.html, mechanics/*.md (adds game mechanics docs)
          - Pipeline: stages/*.md, dataflow/*.mermaid (Mermaid diagrams, no visual mocks)
          The Vision Hasher's vision-summary.md Feature Index will list the correct artifact types for each project type. Use those instead of hardcoding GUI paths.

          Also include paths to:
          - Paths to ALL files in the planning/ directory — list the directory and include every .md file found. The FD Architect uses these to understand the architecture and abstractions.
        </description>
      </element>
      <element name="completion_instruction">
        <description>
          Always include this exact text: "When you have fully completed Phase 5, updated the partitioned knowledge base (agent-workspace/knowledge/), and your feature is archived and committed to its local branch, use attempt_completion to report back to me. Include in your summary: the feature name, branch name (or 'none — git skipped'), total steps completed, location of the archived plan, a per-file summary of what was added to each partitioned knowledge file (architecture.md, database.md, api-routes.md, frontend-ui.md), and your own scope-drift assessment: whether the delivered feature matches the acceptance criteria YOUR ARCHITECT derived from the vision docs, or diverges from them. If it diverges, state which acceptance criteria were affected, whether the adjustment was minor (plan edited in-place) or major (plan re-drafted via Architect), and why the divergence was necessary."
        </description>
      </element>
      <element name="context_about_previous_features">
        <description>
          Briefly mention what features have already been built (from current-state.md "What the App Can Currently Do" section) so the feature-developer understands the existing foundation.

          Also include the wave structure context: which Wave 1 abstractions exist, which Wave 2 skeleton is in place, and the current wave name. Format: "Wave 1 (Foundation) complete: [list abstractions from planning/abstractions.md]. Wave 2 (Skeleton) complete: [list]. Current wave: Wave 3 (Vertical Slices)."

          PRUNING RULE: If more than 3 prior features exist, do NOT copy all bullet points from current-state.md. Instead summarize concisely: "Features 1–N have built: [high-level summary of domains, e.g., auth, user management, inventory CRUD]." Keep this section under 5 bullet points or 2 sentences to avoid context bloat in the FD prompt and its Architect subtask.
        </description>
      </element>
      <element name="feature_roadmap_summary">
        <description>
          Read roadmap.md and compile a concise list of ALL features — one line per feature grouped by wave.
          Format example:
          Wave 1 (Foundation):
            [x] Feature 1: Data Model & Abstractions — Core interfaces and entities
            [x] Feature 2: Project Scaffold & CI/CD — Build pipeline and test harness
          Wave 2 (Skeleton):
            [x] Feature 3: App Shell & Routing — Navigation frame and auth
          Wave 3 (Vertical Slices):
            [ ] Feature 4: User Registration — Signup flow with email verification
            → Feature 5: Task Management — CRUD for tasks with drag-drop ← YOU ARE HERE
            [ ] Feature 6: Task Assignment — Assign tasks to users
          Wave 4 (Cross-Cutting):
            [ ] Feature 7: Search & Filtering — Global search across entities
            [ ] Feature 8: Permissions & Roles — Role-based access control

          Mark completed features with [x], pending features with [ ], and the current feature with → ← YOU ARE HERE. Always include ALL features from roadmap.md — the FD needs visibility into what's coming after this feature, not just what's already built.

          PRUNING RULE: If there are more than 15 total features, collapse completed waves to a one-line summary (e.g., "Wave 1 (Foundation): Features 1-4 complete ✓") and only expand the current wave and future waves in full.

          This gives the FD the big picture — what was built before, what's being built now, and what's coming next. The FD uses this for:
          - Scope drift detection: when drift touches files or abstractions that future features will modify, the FD can identify this and report it
          - User requests: when the user asks for something mid-feature that aligns with a future feature, the FD can recognize it and suggest deferring to the appropriate feature
          - Context for the Architect: the FD passes the relevant parts of the roadmap to the Architect so the implementation plan doesn't conflict with future features
        </description>
      </element>
      <element name="hands_off_mode">
        <description>
          Read hands_off_mode from state.json. Pass as literal boolean in the FD spawn prompt: "hands_off_mode: true" or "hands_off_mode: false". FD uses this to skip user interaction during feature development — Model smoke tests are self-verified by Code, HUMAN/SHT smoke tests are collected for batch presentation at feature end.
        </description>
      </element>
      <element name="smoke_test_automation">
        <description>
          Read the full smoke_test_automation block from state.json. Pass the entire block (enabled, environments with all fields including mcp_available/mcp_name/verified, missing_prerequisites) to FD as literal config in the spawn prompt. FD stores this in its state.json and passes it to Code mode subtasks. If smoke_test_automation.enabled is false or not yet configured, pass enabled: false.
        </description>
      </element>
    </required_elements>
  </feature_developer_prompt_construction>

  <test_fix_prompt_pattern>
    <name>Code Subtask Prompt for Unit Test Fixes</name>
    <description>
      Canonical prompt to use when spawning a code subtask to fix failing unit/integration tests in Phase 4, Step 1. This does NOT cover E2E test failures — those are handled by a separate error recovery path.
    </description>
    <template>
      The test suite is failing after the latest feature integration. Run the full test suite (unit AND integration, unfiltered — never --filter or file path arguments), analyze the failures, and fix the regressions. Do NOT modify feature code unless it directly causes test failures. Do NOT touch E2E tests in tests/e2e/ — those are the PD's domain. Report back with what was broken and what you fixed.
    </template>
  </test_fix_prompt_pattern>

  <knowledge_backfill_prompt_pattern>
    <name>Architect Subtask Prompt for Knowledge Base Backfill</name>
    <description>
      Canonical prompt to use when spawning an architect subtask to backfill knowledge files that the feature-developer failed to update.
    </description>
    <template>
      The feature-developer completed Feature [X] but may not have fully updated the partitioned knowledge base. Review agent-workspace/feature/archive/plan-[sanitized-branch-name].md and agent-workspace/feature/archive/ref-[sanitized-branch-name].md (if they exist), then update the relevant files in agent-workspace/knowledge/ with any missing architectural patterns, schemas, endpoints, or UI components.

      IMPORTANT: Sanitize the branch name before constructing these paths — replace all "/" with "-" (e.g., "feature/user-auth" becomes "feature-user-auth") to match the feature-developer's archive naming convention.

      If git automation was disabled for this feature (no branch name available), sanitize the feature name instead: lowercase, replace spaces with hyphens — to match the feature-developer's alternative archive naming convention.

      When complete, use attempt_completion to report your results. Include in your summary: which knowledge files you updated and what you added to each.
    </template>
  </knowledge_backfill_prompt_pattern>

  <roadmap_revision_prompt_pattern>
    <name>Architect Subtask Prompt for Roadmap Revision</name>
    <description>
      Canonical prompt to use when the user wants to revise the roadmap, or when vision re-evaluation triggers a re-plan of remaining features.
    </description>
      [IF RE-PLANNING AFTER VISION DRIFT: The vision documents have been updated after building Features 1 through [N]. Revise the roadmap for the REMAINING features only (Features [N+1] onward). Preserve the wave structure. Re-evaluate generalization: new integration points may need abstraction.

      CRITICAL — Refresh ALL planning/ files AND tech-sourcing.md to reconcile with as-built reality:
      1. Read ALL files in the knowledge/ directory — list the directory and read every .md file found. These represent what WAS ACTUALLY BUILT.
      2. Read ALL files in the planning/ directory — list the directory and read every .md file found. These represent the ORIGINAL DESIGN INTENT.
      3. Read agent-workspace/project-director/tech-sourcing.md — the original technology sourcing decisions.
      4. For each planning/ file, reconcile: where knowledge/ (as-built) diverges from planning/ (design intent), knowledge/ takes precedence. Update the planning/ file to reflect what was actually built, while preserving any forward-looking design intent for remaining features.
      5. Check tech-sourcing.md: do the technology sourcing decisions still hold for the remaining features? If new integration points from the re-plan require technologies not covered in tech-sourcing.md, add sourcing recommendations for them. If a previously chosen technology was replaced in practice (visible in knowledge/), update the sourcing decision to match reality.
      6. Update agent-workspace/project-director/roadmap.md.]

      [IF USER FEEDBACK REVISION: The user has provided feedback on the roadmap. Revise agent-workspace/project-director/roadmap.md based on this feedback: [USER FEEDBACK]. Preserve wave structure and feature numbering where possible.]

      When complete, use attempt_completion to complete your task.
    </template>
  </roadmap_revision_prompt_pattern>

  <execution_plan_critic_prompt>
    <name>Execution Plan Critic Subtask Prompt</name>
    <description>
      Canonical prompt to use when spawning an Architect-mode Critic subtask in Phase 2,
      Step 6 to review the planning/ directory and roadmap.md before the generalization audit.
    </description>
    <usage>
      Spawn via new_task in architect mode. The Critic reviews the planning/ directory files and
      roadmap.md for architectural flaws, wave dependency integrity, abstraction coverage,
      vision alignment, and feature ordering.
    </usage>
    <template>
      "CRITICAL RULE: You must NEVER read, edit, or modify agent-workspace/project-director/state.json. That file is strictly managed by the Orchestrator.

You are a strict Principal Engineer acting as a Design Critic. Review and polish the planning/ directory files and `agent-workspace/project-director/roadmap.md`.

Perform a strict architectural review across five dimensions:

1. WAVE DEPENDENCIES: Does every Wave 3 feature reference the Wave 1-2 abstractions it needs (entities from Wave 1 data model, app shell/skeleton from Wave 2)? Are there any circular dependencies between features? Can each feature's dependencies actually be built before the feature itself?

2. ABSTRACTION COVERAGE: Are there integration points without provider interfaces in planning/abstractions.md? Every external service integration (LLM, payments, email, storage, auth) must have a defined abstraction interface. Flag any concrete vendor integration without a preceding abstraction.

3. VISION ALIGNMENT: Does every roadmap feature trace back to a vision requirement? Are there vision requirements with no roadmap feature? Flag orphaned vision requirements (in vision docs but not roadmap) and unmoored roadmap features (in roadmap but not vision).

4. FEATURE ORDERING: Do dependencies flow forward (later features depend on earlier features, never the reverse)? Can features actually be built in the specified order? Are there features that depend on features in a later wave?

5. PLANNING/ COMPLETENESS: Are all planning/ files present and internally consistent? List the directory to discover all files, then verify: do abstraction interfaces in abstractions.md match the integration points in integration-points.md? Does the tech stack in tech-stack.md match the architecture described in architecture.md? Does the E2E authoring guide's framework choice align with the tech stack? Are there any broken cross-references between files?

Edit the files to fix any flaws you find. For vision gaps (orphaned requirements or unmoored features), flag them in your completion summary — do NOT edit vision docs.

CRITICAL: If you found ZERO flaws and made ZERO changes, use attempt_completion immediately. HOWEVER, if you made ANY changes, use ask_followup_question with exactly 2 Suggested Responses: 'Approved, proceed' and 'Revert changes'. Include: 'If you want manual adjustments, please type your feedback in the chat.' Do NOT use attempt_completion until the user explicitly approves or reverts. If user clicks 'Approved, proceed', use attempt_completion with a success summary. If user clicks 'Revert changes', undo your edits and use attempt_completion with the message 'CHANGES REVERTED BY USER'."
    </template>
  </execution_plan_critic_prompt>

  <e2e_test_generation_prompt>
    <name>E2E Test Generation Prompt</name>
    <description>
      Canonical prompt to use when spawning a Code mode subtask to generate E2E regression tests for a just-completed feature. The E2E generation subtask analyzes the project type and selects the appropriate E2E framework — the PD does NOT prescribe Playwright or any specific tool.
    </description>
    <usage>
      Spawn via new_task in Code mode. The prompt must include the feature name (sanitized), the E2E spec from the roadmap (written by the Architect during wave map creation), tech stack context, and target file path.
    </usage>
    <template>
      <section name="exception_note" priority="critical">
        ⚠️ EXCEPTION TO YOUR NORMAL WORKFLOW: You are being spawned specifically to generate and run E2E tests. Your normal rule of "E2E tests are NEVER run during the quality loop" does NOT apply here. This subtask's purpose IS E2E test creation and verification. You MUST create the test file and you MUST run ALL E2E tests before completion.
      </section>

      <section name="context">
        <item number="1">
          <title>Project Tech Stack</title>
          [Copy the tech stack from the planning/ directory (planning/tech-stack.md) and relevant architecture details from agent-workspace/knowledge/architecture.md. Include: programming language, UI framework (web/WPF/CLI/mobile/game/library), build system, test runner already in use.]
        </item>
        <item number="2">
          <title>Feature to Test</title>
          Feature name: [Feature Name]
          Sanitized name for file: [sanitized-feature-name]
        </item>
        <item number="3">
          <title>E2E Specification (from Roadmap — written by Architect during wave map)</title>
          [Copy the EXACT E2E spec from the roadmap.md entry for this feature. This is the authoritative source for what the E2E test must verify. The Architect wrote this during Phase 2 execution planning specifically for the regression test suite.]

          If the roadmap E2E spec is "Not applicable — [reason]": do NOT generate an E2E test. Instead, use attempt_completion with: E2E Framework: None, Run Command: None, Test File: None, Reason: [copy the "Not applicable" reason from the roadmap]. This is a valid outcome — not all project types need traditional E2E tests.
          
          (Note: The PD filters "Not applicable" features before spawning you. Under normal operation, you should never receive a "Not applicable" spec. This branch exists as a defensive fallback in case the PD's filtering has a bug.)
        </item>
      </section>

      <section name="instructions">
        <item number="0" priority="critical">
          <title>Read the E2E Authoring Guide (MANDATORY — Before Any Code)</title>
          Read agent-workspace/project-director/planning/e2e-authoring-guide.md BEFORE writing any test code. This guide encodes the project's E2E conventions: fixture patterns, test infrastructure (test DB, countdown window), selector strategies, helper conventions, timeout rules, self-cleaning test patterns, and the quick-reference checklist. Every E2E test you write MUST follow these conventions. If the guide specifies a framework: use that framework — do not select a different one. If the guide specifies selector conventions (e.g., AutomationId naming for XAML): follow them exactly. If the guide specifies a hands-off countdown for desktop projects: note it in your Setup Notes but do NOT implement it — the fixture handles it. If the guide does NOT exist (planning/ directory missing or guide not yet generated): note this as a warning in your completion summary but proceed — the PD will reconcile later.
        </item>
        <item number="1">
          <title>Analyze the Project Type and Select E2E Framework</title>
          Check the E2E authoring guide first — if it already specifies a framework, use that framework. Otherwise, based on the tech stack, determine the project type and choose the appropriate E2E testing framework:
          - Web applications (React, Next.js, Vue, Svelte, Angular, plain HTML/JS): Evaluate Playwright, Cypress, or other browser-testing frameworks and choose the one best suited to the project's existing tooling and conventions
          - WPF/WinUI/.NET desktop applications: Use WinAppDriver with FlaUI, or a custom test harness with UI Automation
          - CLI/terminal applications: Use shell scripts, expect, or a custom test harness
          - Mobile applications: Use Appium or Maestro
          - API-only backends: Use Supertest, REST Client, or HttpClient-based test projects
          - Game/Unity projects: Use Unity Test Framework with playmode tests, or a custom test harness
          - Libraries/SDKs: Use integration test projects that exercise the public API
          - If the project already has an E2E framework configured (check tests/e2e/ for existing tests), use the SAME framework — do not introduce a second E2E tool
          - If the project has no existing E2E setup: choose the most appropriate framework for the project type and set it up from scratch
        </item>
        <item number="2">
          <title>Set Up the E2E Framework</title>
          Install dependencies, configure the test runner, and add a run script to the project (e.g., package.json "test:e2e" script, .csproj test project, Makefile target). Ensure the framework can be run with a single command. If the framework requires a running application server, note this in your report.
        </item>
        <item number="3">
          <title>Read the Feature Code and UI Structure</title>
          Read the feature's code, UI markup, and component structure to understand DOM elements, component IDs, accessibility labels, and interaction patterns. This is critical for writing accurate selectors. If knowledge files exist (agent-workspace/knowledge/frontend-ui.md), read them for UI context. If unit tests exist (tests/unit/), read them to understand what's already covered — E2E tests should complement unit tests, not duplicate them.
        </item>
        <item number="4">
          <title>Write the E2E Test</title>
          Write a comprehensive E2E test at the target path: tests/e2e/feature-[sanitized-name].[ext] (choose the extension appropriate for the framework: .spec.ts for Playwright, .cs for .NET, .py for Python, etc.).
          The test MUST:
          - Verify EVERY point in the roadmap E2E spec provided above
          - Use the feature name in the test description for traceability
          - Use reliable selectors (prefer data-testid, accessibility labels, or stable IDs over CSS classes)
          - Include setup and teardown that doesn't interfere with other tests
          - Follow the existing project's coding conventions

          CRITICAL: You MUST create the test file. If you cannot set up the framework or cannot determine the project type, report the specific blocker in your completion summary — do NOT complete without creating the file or explaining exactly why it's impossible.
        </item>
        <item number="5">
          <title>Run ALL E2E Tests Before Completion</title>
          Run the FULL E2E test suite — not just the new test you wrote, but ALL existing E2E tests in tests/e2e/. This verifies your new test works AND that it doesn't break any existing tests.
          
          If any test fails: fix it. If the failure is in your new test: fix the test code. If the failure is in an existing test: this is a regression — your new feature may have broken something. Report this in your completion summary under "Warnings" so the PD can investigate.
          
          If the test framework requires a running server/app and none is running: start the app, run the tests, then note the startup requirement in your report.
        </item>
        <item number="6">
          <title>Report Back</title>
          Use attempt_completion with this exact structure in your summary:
          - E2E Framework: [framework name and version, or "None" if not applicable]
          - Run Command: [exact command to run the E2E suite, or "None"]
          - Test File: tests/e2e/feature-[sanitized-name].[ext], or "None — not applicable: [reason]"
          - Acceptance Criteria Covered: [list each criterion and how the test verifies it]
          - Setup Notes: [any prerequisites, e.g., "requires app running on localhost:3000" or "requires WinAppDriver service running"]
          - Warnings: [any limitations or known issues]
        </item>
        <item number="7">
          <title>Launch QA Reviewer Before Completion</title>
          Before calling attempt_completion, spawn a QA Reviewer subtask (qa mode) to review your E2E test code. The QA Reviewer provides an independent read-only review of the test file for bugs, weak assertions, missing edge cases, and framework misuse. Evaluate each finding: fix valid issues, justify any dismissals. The QA review is automatic — do not ask for consent.
        </item>
        <item number="8">
          <title>Debug Subtask on Three Failures</title>
          If you go through 3 full cycles of generating the E2E test and it still fails (file not created correctly, tests don't pass, or the framework setup is broken after 3 genuine fix attempts), launch a Debug subtask (debug mode) with: the original E2E spec from the roadmap, the current state of the test file (if any), all error output from the 3 attempts, and the framework setup details. The Debug subtask investigates systematically with a fresh perspective and reports root cause analysis. Do NOT loop more than 3 times without the Debug subtask.
        </item>
        <item number="9">
          <title>Database Safety — Do Not Delete User Data</title>
          If the E2E test interacts with a database (seeds data, creates records, modifies schema):
          - NEVER delete, drop, or truncate the user's existing database. The E2E test must use a separate test database, or transactions that roll back, or seed data in isolated tables/collections that do not conflict with real user data.
          - If the test creates new fields, records, tables, or collections: clean them up in teardown so no traces remain after the test run. The database must be returned to its pre-test state.
          - If the E2E framework or project setup does not support test isolation: flag this in your completion summary under Warnings with a clear explanation of what database changes were made and why they couldn't be cleaned up.
        </item>
      </section>
    </template>
  </e2e_test_generation_prompt>

  <dashboard_templates>
    <name>Dashboard Templates</name>
    <description>
      Templates for the Project Director's living dashboard files. Created in Phase 1 Step 2
      and updated throughout the project lifecycle. These are distinct from vision templates —
      they track project state, not product vision.
    </description>

    <template name="backlog.md">
      <content><![CDATA[
# Backlog (Icebox)

Scope-creep ideas and deferred features live here...

| # | Idea | Proposed By | Date |
|---|------|-------------|------|
]]></content>
    </template>

    <template name="current-state.md">
      <content><![CDATA[
# Current State Dashboard

## 1. What the App Can Currently Do

_Initialized — awaiting vision discovery._

## 2. UI Map

_Not yet available._

## 3. Architectural Health & Tech Debt

_No assessment yet._

## 4. Vision Updates

_No changes yet — vision docs are the current authority._

## 5. E2E Regression Suite

_Not yet configured._
]]></content>
    </template>
  </dashboard_templates>

  <ad_hoc_code_subtask_prompt>
    <name>Code Subtask Prompt for Ad-Hoc Changes</name>
    <description>
      Canonical prompt to use when spawning a Code mode subtask directly (bypassing FD) for small ad-hoc
      changes: bug fixes, single-file refactors, minor logic adjustments. The PD MUST include the roadmap/
      vision context so the Code subtask understands where this change fits in the larger project.
    </description>
    <usage>
      Spawn via new_task in Code mode. The prompt must include: the user's exact request, roadmap context
      (which feature this relates to, wave structure), relevant vision context, and the current state of
      the project.
    </usage>
    <template>
      <section name="context">
        <item number="1">
          <title>Project Context</title>
          [Copy the project vision from vision-summary.md and the tech stack from planning/tech-stack.md. Include the wave structure: which Wave 1 abstractions exist, which Wave 2 skeleton is in place, which wave we're currently in.]
        </item>
        <item number="2">
          <title>Where This Fits</title>
          [Explain where this ad-hoc request fits in the roadmap. If it's covered by an upcoming feature: name the feature and wave. If it's new: explain why it's being handled directly rather than via FD.]
        </item>
        <item number="3">
          <title>What's Already Built</title>
          [Brief summary from current-state.md "What the App Can Currently Do" — keep to 2-3 bullet points.]
        </item>
      </section>

      <section name="instructions">
        <item number="1">
          <title>Understand the Request</title>
          Read the user's request and the project context. Understand where this change fits in the larger application. If the request is ambiguous, ask one focused clarifying question — do not over-analyze.
        </item>
        <item number="2">
          <title>Make the Change</title>
          Implement the requested change. Keep it minimal and targeted — do not expand the scope beyond what was asked. If you discover that the change requires touching shared infrastructure (interfaces, provider abstractions, core data model), flag this in your completion summary as "ARCHITECTURAL IMPACT" and explain why — the PD will need to reconcile this with the roadmap.
        </item>
        <item number="3">
          <title>Run Tests</title>
          Follow your standard quality loop: implement → run all automated tests (unit AND integration, unfiltered, never --filter or file path arguments) → self-refactor → run all automated tests again. If tests fail: fix only if your change directly caused the failure. If pre-existing tests fail: flag in your completion summary — do NOT fix unrelated tests.
        </item>
        <item number="4">
          <title>Report Back</title>
          Use attempt_completion with this structure in your summary:
          - What was changed: [file paths and brief description of each change]
          - Tests: [all passed / N failed (which ones, why)]
          - Architectural Impact: [none / description of any shared infrastructure touched]
          - Scope Notes: [any observations about how this change relates to the larger project]
        </item>
      </section>
    </template>
  </ad_hoc_code_subtask_prompt>

  <!-- ============================================================ -->
  <!-- PATH B VISION REALIGNMENT PIPELINE PROMPTS                     -->
  <!-- ============================================================ -->

  <vision_hasher_update_prompt>
    <name>Vision Hasher Prompt for Vision File Updates (Path B Step B3)</name>
    <description>
      Canonical prompt to use when spawning a Vision Hasher to update existing vision files
      during the Path B Vision Realignment Pipeline. The VH must read all current vision files,
      interview the user about the changes, and update everything. It must report a delta
      between what was proposed and what actually changed.
    </description>
    <usage>
      Spawn via new_task in Vision Hasher mode. Include: the aggregated change summary from
      Step B2, paths to ALL current vision files, and instruction to interview the user.
    </usage>
    <template>
      <section name="context">
        <item number="1">
          <title>What Changed</title>
          [Insert the aggregated change summary from Step B2: what changes, what stays the same, which remaining roadmap features are affected.]
        </item>
        <item number="2">
          <title>Current Vision Files</title>
          [List ALL vision files the VH must read before interviewing the user. Include every .md file, every deliverable (HTML mocks, OpenAPI YAML, session transcripts, usage examples, Mermaid diagrams), and vision-config.json.]
        </item>
        <item number="3">
          <title>Project Context</title>
          [Brief summary: what's been built so far (features 1 through current_feature_index), current wave, tech stack from planning/tech-stack.md.]
        </item>
      </section>

      <section name="instructions">
        <item number="1">
          <title>Read Everything First</title>
          Read ALL current vision files listed above. Understand the complete current vision before making any changes. Do NOT skip this step — you must know what exists before you can update it.
        </item>
        <item number="2">
          <title>Interview the User</title>
          Using your standard guided interview methodology, discuss the proposed changes with the user. Confirm each change. Explore implications: "If we change X, does that affect Y?" You may discover implications not in the change summary — explore them with the user. The user may change their mind during the interview — that's expected.
        </item>
        <item number="3">
          <title>Update ALL Affected Vision Files</title>
          Update every vision file that needs to change. This includes: per-artifact specs (features/, screens/, data/, flows/), atomic templates (app-overview.md, personas.md, feature-inventory.md, edge-cases.md), deliverable files (HTML mocks, OpenAPI YAML, session transcripts, usage examples, Mermaid diagrams), and vision-summary.md. Do NOT leave any file in an inconsistent state.
        </item>
        <item number="4">
          <title>Report the Delta</title>
          In your completion summary, report EXACTLY what changed vs. the original proposal:
          - What was added beyond the proposal
          - What was modified differently than proposed
          - What was deferred ("⚠️ INCOMPLETE" with reason)
          - Which vision files were modified (complete list with paths)
          - Which mocks/deliverables changed (user should review these visually)
          - Any concerns or contradictions you noticed but couldn't resolve (flag for the VH Critic in the next step)
        </item>
      </section>
    </template>
  </vision_hasher_update_prompt>

  <vision_hasher_critic_prompt>
    <name>Vision Hasher Critic Prompt (Path B Step B4)</name>
    <description>
      Canonical prompt to use when spawning a Vision Hasher in a CRITIC role to audit
      recently-updated vision files for internal contradictions. This is a focused audit —
      NOT a discovery session. The VH must not add new features or expand scope.
    </description>
    <usage>
      Spawn via new_task in Vision Hasher mode. The prompt explicitly instructs the VH to
      act as a critic, not a discoverer.
    </usage>
    <template>
      <section name="role_override">
        CRITICAL: You are acting as a VISION CRITIC, not a Vision Hasher. Your role today is to audit completed vision documents for internal consistency. Do NOT conduct discovery interviews. Do NOT suggest new features. Do NOT expand scope. Only find and fix contradictions in existing content.
      </section>

      <section name="context">
        <item number="1">
          <title>What Just Happened</title>
          The vision files were updated to reflect a mid-project change. [Brief summary of the change from Step B2/B3.] All vision files should now be internally consistent — your job is to verify and fix any remaining issues.
        </item>
        <item number="2">
          <title>Vision Files to Audit</title>
          [List ALL vision files — the VH Critic must read every one.]
        </item>
      </section>

      <section name="instructions">
        <item number="1">
          <title>Read ALL Vision Files</title>
          Read every vision file. Build a mental model of the complete application as described by these files.
        </item>
        <item number="2">
          <title>Find Contradictions</title>
          Search for:
          - Features described differently in different files (feature-inventory.md says one thing, features/[name].md says another)
          - Orphaned references (a screen references a data entity that doesn't exist in data/)
          - Edge cases that contradict feature descriptions
          - Screens that describe behaviors not covered by any feature spec
          - Data entities referenced by features but not defined in data/
          - Inconsistent terminology (same concept named differently across files)
          - Deliverable files (HTML mocks, OpenAPI YAML, etc.) that don't match their paired .md specs
        </item>
        <item number="3">
          <title>Fix What You Can</title>
          For each contradiction found, determine the correct resolution and fix the files. Prefer the more recent/specific description when two files conflict. Update both files if needed for consistency.
        </item>
        <item number="4">
          <title>Flag What You Can't Fix</title>
          If a contradiction requires NEW discovery to resolve (not just reconciliation of existing content): do NOT resolve it yourself. Flag it as "DEFERRED — requires discovery: [description of the ambiguity and what needs to be decided]." These will be presented to the user.
        </item>
        <item number="5">
          <title>Report</title>
          In your completion summary:
          - Number of contradictions found and fixed (with file paths)
          - Number of DEFERRED items (with descriptions)
          - Any patterns you noticed (e.g., "3 features all reference 'user preferences' differently")
          - Do NOT propose new features or scope expansions
        </item>
      </section>
    </template>
  </vision_hasher_critic_prompt>

  <realignment_architect_prompt>
    <name>Architect Prompt for Planning/Roadmap/Knowledge Updates (Path B Step B5)</name>
    <description>
      Canonical prompt to use when spawning an Architect to propagate vision changes through
      planning files, roadmap, and knowledge files during the Path B Vision Realignment Pipeline.
      This is the largest spawn in the pipeline — the Architect must read everything and update
      everything.
    </description>
    <usage>
      Spawn via new_task in Architect mode. Include: the vision change delta from Step B3,
      paths to ALL vision files, ALL planning files, roadmap.md, and knowledge/ files.
    </usage>
    <template>
      <section name="context">
        <item number="1">
          <title>Vision Changes</title>
          [Insert the VH delta report from Step B3: exactly what changed in the vision files, including which files were modified and the nature of each change.]
        </item>
        <item number="2">
          <title>Current Project State</title>
          - Features built: 1 through [current_feature_index]
          - Current wave: [Wave N]
          - Tech stack: [from planning/tech-stack.md]
          - [Brief summary from current-state.md]
        </item>
        <item number="3">
          <title>Files You Must Read</title>
          - ALL vision files in agent-workspace/project-director/vision/
          - ALL planning files in agent-workspace/project-director/planning/
          - roadmap.md
          - ALL knowledge files in agent-workspace/knowledge/
        </item>
      </section>

      <section name="instructions">
        <item number="1">
          <title>Read Everything</title>
          Read ALL vision files, ALL planning files, roadmap.md, and ALL knowledge files. Build a complete understanding of: (a) what the vision now says, (b) what the planned architecture looks like, (c) what has actually been built (from knowledge/ files).
        </item>
        <item number="2">
          <title>Update the Roadmap</title>
          Revise all FUTURE features (current_feature_index + 1 onwards) to match the updated vision. Do NOT modify past feature entries (1 through current_feature_index) — those represent what was already built. Maintain wave groupings. Update feature names, 1-line descriptions, and dependencies as needed. If a future feature is now irrelevant given the vision change: remove it. If the vision change requires new features: add them in the appropriate wave.
        </item>
        <item number="3">
          <title>Determine Realignment Need</title>
          Compare the updated vision against what was actually built (knowledge/ files). Is there a gap? Specifically:
          - Does the new vision require behavior that existing code doesn't support?
          - Do existing abstractions need their interfaces changed to support the new vision?
          - Were any already-built features invalidated by the vision change?

          If YES to any: design a REALIGNMENT FEATURE. Insert it as the immediate next feature (current_feature_index + 1). This feature:
          - Retrofits existing code to match the new vision
          - Updates affected abstractions, data models, and integration points
          - Does NOT build new capabilities — it only aligns what exists with the new vision
          - Includes updating any E2E tests invalidated by the vision change
          - Name it descriptively: "Feature [N]: Realignment — [what changed]"

          If NO gap exists (future roadmap changes are sufficient): do NOT insert a Realignment Feature. Note in your completion summary why one isn't needed.
        </item>
        <item number="4">
          <title>Update ALL Planning Files</title>
          Every planning/ .md file that references changed vision concepts must be updated:
          - architecture.md: if the vision change affects component design or data flow
          - tech-stack.md: if new technology decisions are implied by the vision change
          - abstractions.md: if provider interfaces need new methods or new abstractions are needed
          - data-model.md: if data entities changed, were added, or were removed
          - integration-points.md: if external integrations changed
          - platform-notes.md: if platform-specific implications changed
          - e2e-authoring-guide.md: if E2E patterns or conventions changed
          - planning-summary.md: update to reflect all changes
        </item>
        <item number="5">
          <title>Update Knowledge Files</title>
          This is an explicit exception to the normal boundary (PD audits knowledge/ but never edits). During realignment, YOU are authorized to update knowledge/ files to reflect the new vision. Update any knowledge file whose described architecture, patterns, or conventions have changed.
        </item>
        <item number="6">
          <title>Flag E2E Impact</title>
          Identify which existing E2E tests (tests/e2e/) are invalidated by the vision change. List each test file and why it's affected. If a Realignment Feature was inserted, its E2E spec must include updating these tests.
        </item>
        <item number="7">
          <title>Report</title>
          In your completion summary:
          - Roadmap changes: features added/removed/modified (with before/after)
          - Realignment Feature: [inserted / not needed — why]
          - Planning files updated: [list with brief description of each change]
          - Knowledge files updated: [list]
          - E2E tests invalidated: [list with reasons]
        </item>
      </section>
    </template>
  </realignment_architect_prompt>

  <realignment_critic_prompt>
    <name>Architect Critic Prompt for Realignment Review (Path B Step B6)</name>
    <description>
      Canonical prompt to use when spawning an Architect in CRITIC role to independently review
      all changes made during the Vision Realignment Pipeline (Steps B3-B5).
    </description>
    <usage>
      Spawn via new_task in Architect mode. The prompt explicitly instructs the Architect to
      act as a strict Principal Engineer / Design Critic.
    </usage>
    <template>
      <section name="role_override">
        CRITICAL: You are acting as a PRINCIPAL ENGINEER / DESIGN CRITIC, not an Architect. Your role today is to audit completed planning and roadmap documents for quality and consistency. Do NOT propose new features. Do NOT expand scope. Only find and report issues in existing content.
      </section>

      <section name="context">
        <item number="1">
          <title>What Just Happened</title>
          A mid-project vision realignment was performed: vision files were updated (VH), contradictions were fixed (VH Critic), and planning/roadmap/knowledge files were updated (Architect). Your job is to independently audit the result.
        </item>
        <item number="2">
          <title>Files to Audit</title>
          - ALL vision files in agent-workspace/project-director/vision/
          - ALL planning files in agent-workspace/project-director/planning/
          - roadmap.md
          - ALL knowledge files in agent-workspace/knowledge/
        </item>
      </section>

      <section name="instructions">
        <item number="1">
          <title>Read Everything</title>
          Read ALL files listed above. Build a complete understanding of the vision, the planned architecture, the roadmap, and what has been built.
        </item>
        <item number="2">
          <title>Audit for Consistency</title>
          Check these specific dimensions:
          - Vision-Planning Alignment: Does every changed vision concept have corresponding updates in planning files? Are there planning references to vision concepts that no longer exist?
          - Abstraction Coverage: Are there new/changed integration points without provider interfaces? Do existing abstractions still cover what the new vision requires?
          - Roadmap Integrity: Do all forward dependencies hold? Does any future feature depend on something that was removed or changed? Is the Realignment Feature (if inserted) correctly positioned and scoped?
          - Realignment Completeness: If a Realignment Feature was inserted, does it cover ALL gaps between as-built code and new vision? Are there gaps it misses?
          - Planning Internal Consistency: Do architecture.md, abstractions.md, data-model.md, and integration-points.md agree with each other? No contradictions?
          - E2E Coverage: Are all invalidated E2E tests accounted for in the Realignment Feature's E2E spec?
        </item>
        <item number="3">
          <title>Report Findings</title>
          For each issue found, report: the specific files involved, what the contradiction or gap is, and a suggested resolution. Categorize as:
          - CRITICAL: Will cause build failures, broken dependencies, or impossible feature sequences
          - HIGH: Missing abstraction, incomplete coverage, dependency violation
          - MEDIUM: Inconsistency that won't block progress but should be fixed
          - LOW: Naming inconsistency, minor documentation gap

          Do NOT make changes yourself. Report findings only — the PD will decide how to resolve them.
        </item>
        <item number="4">
          <title>Scope Discipline</title>
          Do NOT propose new features. Do NOT suggest scope expansions. If you notice an opportunity for improvement that goes beyond fixing consistency: note it as "OBSERVATION (out of scope): [description]" — the PD will decide whether to pursue it separately.
        </item>
      </section>
    </template>
  </realignment_critic_prompt>

  <!-- DEPRECATED PROMPTS — kept for backward compatibility reference -->
  <deprecated_prompts>
    <name>Legacy Prompts (Deprecated)</name>
    <description>
      These prompts have been replaced by the new planning/ directory + wave map approach.
      Kept for reference and backward compatibility with any code that still references them by name.
    </description>

    <standard_prd_roadmap_prompt>
      <name>DEPRECATED — Standard Architect Prompt for PRD and Roadmap Creation</name>
      <description>
        Replaced by planning_directory_prompts + wave_map_prompt + assembly_verification_prompt.
      </description>
    </standard_prd_roadmap_prompt>

    <prd_architecture_prompt>
      <name>DEPRECATED — Architect Prompt for PRD & Architecture</name>
      <description>
        Replaced by planning_directory_prompts. The monolithic PRD is now split into focused planning/ files.
      </description>
    </prd_architecture_prompt>

    <feature_specs_prompt>
      <name>DEPRECATED — Architect Prompt for Feature Specification</name>
      <description>
        Replaced by wave_map_prompt. The roadmap is now a lightweight wave map — no acceptance criteria.
      </description>
    </feature_specs_prompt>

    <feature_specs_continuation_prompt>
      <name>DEPRECATED — Architect Prompt for Feature Specification Continuation</name>
      <description>
        Replaced by wave_map_prompt. One spawn creates the entire wave map.
      </description>
    </feature_specs_continuation_prompt>
  </deprecated_prompts>
</common_patterns>