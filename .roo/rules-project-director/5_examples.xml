<complete_examples>
  <overview>
    End-to-end examples of the Project Director workflow, demonstrating each phase from vision discovery to project completion.
  </overview>

  <example name="phase_1_vision_discovery">
    <scenario>
      The user presents a vague vision: "I want to build a SaaS platform for restaurant inventory management." The PD must first hash out the complete vision before any technical planning.
    </scenario>

    <user_request>Build a SaaS platform for restaurant inventory management.</user_request>

    <workflow>
      <step number="1">
        <title>Phase 1: Initialize and Write Templates</title>
        <approach>
          PD parses the vague vision. Creates state.json with current_phase=1. Creates agent-workspace/project-director/vision/ directory with subdirectories (features/, data/, screens/, flows/) and writes atomic template files (app-overview.md, personas.md, feature-inventory.md, edge-cases.md) using the vision_templates pattern.
        </approach>
      </step>

      <step number="2">
        <title>Phase 1: Spawn Vision Hasher</title>
        <approach>
          PD spawns Vision Hasher via new_task with: template paths, user's vision brief, and constraint: "This is a SaaS web app, multi-tenant, targeting small-to-medium restaurants." Vision Hasher conducts a guided interview, fills all 7 templates, generates HTML wireframes for each screen, and returns with vision-summary.md reporting: 28 features described, 14 UI screens, 8 data entities. 2 flagged concerns: "Real-time inventory sync across multiple locations may be technically challenging" and "User wants offline mode — may conflict with real-time sync."
        </approach>
        <expected_outcome>Complete vision documented in agent-workspace/project-director/vision/. All templates filled, wireframes generated.</expected_outcome>
      </step>

      <step number="3">
        <title>Phase 1: Vision Approval Gate</title>
        <approach>
          PD presents summary: "Vision complete: 28 features, 14 screens, 8 data entities. 2 flagged concerns for technical review." User reviews and clicks "Approved — let's create the execution plan." PD updates state.json: current_phase=2.
        </approach>
        <expected_outcome>Vision approved. Moving to execution planning.</expected_outcome>
      </step>
    </workflow>

    <key_takeaways>
      <takeaway>The PD never conducts the vision interview — Vision Hasher does it.</takeaway>
      <takeaway>Vision templates ensure consistent, comprehensive output across all projects.</takeaway>
      <takeaway>Flagged concerns don't block vision approval — they inform the Architect in Phase 2.</takeaway>
    </key_takeaways>
  </example>

  <example name="phase_2_execution_planning">
    <scenario>
      Vision docs are complete for a ChatGPT clone as a native Windows desktop application (WPF/.NET). The vision describes 24 features: chat interface, streaming responses, conversation history, artifacts (like Claude's), model selection, prompt library, etc. The PD must now create the execution plan with technology sourcing, a planning/ directory, and a lightweight wave map.
    </scenario>

    <workflow>
      <step number="1">
        <title>Phase 2 Step 1: Read Vision Documents</title>
        <approach>
          PD reads all vision docs. Counts: 24 features, 12 screens, 8 data entities. Identifies all integration points and data entities for the technology sourcing study.
        </approach>
      </step>

      <step number="2">
        <title>Phase 2 Step 2: Technology Sourcing Study</title>
        <approach>
          PD sets phase_2_substep="tech_sourcing", spawns Architect with technology_sourcing_prompt. Architect analyzes every component:

          - Chat rendering engine → Custom Build (core differentiator, no existing WPF library matches the streaming markdown rendering needed)
          - LLM integration → Open-Source Library (use an existing .NET OpenAI-compatible client, with provider abstraction)
          - Artifacts feature → Reference Implementation (analyze how claude.ai implements artifacts in the browser; extract content block registry pattern, sandboxed rendering, incremental updates; adapt to WPF via UserControls + WebView2 for sandboxed content)
          - Authentication → SaaS (Auth0 or similar — commodity, not worth building)
          - Database → Open-Source Library (Entity Framework Core + SQLite for local-first desktop)
          - Conversation persistence → Custom Build (specific data model tied to the chat domain)
          - File attachments → Open-Source Library (existing .NET file handling libraries)
          - Syntax highlighting → Open-Source Library (AvalonEdit or similar)

          Architect writes tech-sourcing.md and saves ref-claude-artifacts.md to external-docs/. PD presents summary to user. User reviews the reference implementation approach for artifacts. User approves. PD sets phase_2_substep="architecture".
        </approach>
        <expected_outcome>Technology sourcing decisions documented and approved. Reference implementation analysis saved to external-docs/.</expected_outcome>
      </step>

      <step number="3">
        <title>Phase 2 Step 3: Architecture & Abstractions — Planning/ Directory</title>
        <approach>
          PD spawns Architect with planning_directory_prompts. Batch 1: architecture.md, tech-stack.md, abstractions.md. Architect reads vision docs and tech-sourcing.md, creates the first three planning files:

          - architecture.md: MVVM pattern, dependency injection, plugin-based content block system (from the Claude artifacts reference analysis). High-level component diagram: ChatShell → ContentBlockRegistry → RendererPipeline → LLMProvider → StorageProvider.
          - tech-stack.md: WPF .NET 9, Entity Framework Core, SQLite, CommunityToolkit.Mvvm, Microsoft.Extensions.DependencyInjection. Per-component justifications referencing vision requirements and sourcing decisions.
          - abstractions.md: ILLMProvider (GenerateAsync, StreamAsync → OpenAiProvider), IAuthProvider (LoginAsync, LogoutAsync → Auth0Provider), IStorageProvider (SaveAsync, LoadAsync → LocalFileStorageProvider), IContentBlockRenderer (RenderAsync → MarkdownRenderer, CodeBlockRenderer, ArtifactRenderer).

          PD presents for human discussion. User reviews the abstractions and architecture. User approves. PD spawns Architect for batch 2.

          Batch 2: data-model.md, integration-points.md, platform-notes.md, e2e-authoring-guide.md, planning-summary.md. Architect creates the remaining files. The E2E authoring guide is generated last in this batch — after tech-stack.md and platform-notes.md — so it can reference concrete framework and platform decisions.

          - data-model.md: Conversation, Message, Artifact, PromptTemplate entities with relationships and attributes derived from vision/data/.
          - integration-points.md: OpenAI API, Auth0, local file system — all wrapped by abstractions from abstractions.md with fallback strategies.
          - platform-notes.md: WPF patterns (XAML bindings, DataTemplate, UserControl composition), ClickOnce deployment, auto-update via Squirrel.Windows.
          - e2e-authoring-guide.md: WPF-specific E2E conventions — FlaUI with WinAppDriver, CollectionFixture pattern (one launch for all tests), SQLite test database via MSB_DB_PATH env var, hands-off countdown window (3-second with CI skip), AutomationId selector priority (Nav{Screen}, {Action}{Entity}Button, {Entity}{Field}Input), self-cleaning test pattern, MessageBox handling, quick-reference checklist.
          - planning-summary.md: Index tying all 8 files together with quick-reference tables.

          PD presents for human discussion. User approves all files. PD sets phase_2_substep="wave_map".
        </approach>
        <expected_outcome>All 7 planning/ files created and approved. Architecture and abstractions locked in.</expected_outcome>
      </step>

      <step number="4">
        <title>Phase 2 Step 4: Wave Map</title>
        <approach>
          PD spawns Architect with wave_map_prompt. Architect reads all vision docs and the complete planning/ directory, writes a lightweight roadmap.md:

          Wave 1 (Foundation — 6 features): Project Scaffold & CI/CD, Data Model & Migrations, LLM Provider Abstraction, Auth Provider Abstraction, Storage Provider Abstraction, Content Block Registry.
          Wave 2 (Skeleton — 4 features): App Shell & Navigation, All Screen Shells, Routing & Navigation Service, Theme & Styling.
          Wave 3 (Vertical Slices — 10 features): Chat Message Rendering, Streaming Response Display, Conversation Management, Model Selection, Prompt Input, Markdown Rendering, Code Block, Artifacts System, File Attachments, Conversation Search.
          Wave 4 (Cross-Cutting — 4 features): Permissions/Roles, Auto-Update, Analytics, E2E Test Suite.

          Each feature has ONLY: name, 1-line description, dependencies. NO acceptance criteria. The FD Architect will derive those from vision docs.

          PD presents for human discussion. User reviews the wave structure. User approves. PD sets phase_2_substep="assembly".
        </approach>
        <expected_outcome>Lightweight wave map with 24 features across 4 waves. FD Architect owns acceptance criteria.</expected_outcome>
      </step>

      <step number="5">
        <title>Phase 2 Step 5: Assembly — Numbering & Dependency Check</title>
        <approach>
          PD spawns Architect with assembly_verification_prompt. Architect numbers all features 1-24, verifies forward-flow dependencies, checks abstraction references against planning/abstractions.md. No issues found. PD reads roadmap.md, counts 24 features, updates total_features=24 in state.json. Sets phase_2_substep="critic_review".
        </approach>
      </step>

      <step number="6">
        <title>Phase 2 Step 6: Critic Review</title>
        <approach>
          PD spawns independent Architect as Critic. Critic reviews all 5 dimensions across planning/ directory + roadmap.md. Finds: Feature 12 (Artifacts) doesn't explicitly reference the Wave 1 Content Block Registry it depends on. Also finds planning-summary.md is missing the ContentBlockRenderer from the quick-reference abstractions list. Critic fixes both. User approves. PD sets phase_2_substep="generalization_audit".
        </approach>
      </step>

      <step number="7">
        <title>Phase 2 Steps 7-8: Generalization Audit & Approval</title>
        <approach>
          PD scans roadmap. All integration points are properly abstracted — the tech sourcing step caught these early. No issues. PD sets phase_2_substep="approval_gate". Presents summary: ".NET 9 WPF desktop app. 24 features in 4 waves. Key sourcing decisions: custom chat engine, reference-impl artifacts (Claude pattern), SaaS auth, open-source data layer." User approves. PD configures smoke testing (desktop — Windows-MCP), sets current_phase=3, phase_2_substep=null.
        </approach>
      </step>
    </workflow>

    <key_takeaways>
      <takeaway>Technology sourcing is a dedicated step BEFORE architecture — sourcing decisions drive the architecture.</takeaway>
      <takeaway>The planning/ directory replaces the monolithic PRD — each file is a focused topic, easier for the FD Architect to consume.</takeaway>
      <takeaway>The wave map is lightweight: feature names, 1-line descriptions, dependencies only. The FD Architect derives acceptance criteria from vision docs.</takeaway>
      <takeaway>Phase 2 spawns 5-7 Architects total: 1 for tech sourcing, 2-3 for planning/, 1 for wave map, 1 for assembly, 1 for critic.</takeaway>
      <takeaway>phase_2_substep enables precise resume if context is lost mid-planning.</takeaway>
    </key_takeaways>
  </example>

  <example name="full_build_loop">
    <scenario>
      With vision approved and execution plan in place, the PD begins the build loop.
    </scenario>

    <workflow>
      <step number="1">
        <title>Routine Loop: Phase 3 → 4 → 5</title>
        <approach>
          For each feature, the PD spawns FD (Phase 3), runs unit/integration tests + QA-generated E2E tests + architectural audit + vision re-evaluation (Phase 4), then presents the PR/merge gate (Phase 5). Features 1–14 proceed cleanly through this loop with no drift.
        </approach>
      </step>

      <step number="2">
        <title>Phase 4 Re-evaluation: Vision Drift After Feature 15</title>
        <approach>
          Feature 15 (Inventory Alerts) reveals that the vision's "real-time alerts" concept requires WebSocket infrastructure that wasn't in the Wave 1 data model. This is minor drift: the data model needs a new Notification entity that wasn't in the original vision.

          PD creates vision/data/notification.md: adds Notification entity with attributes. Updates vision/feature-inventory.md: adds "Real-time notification delivery" to the alerts feature description. Notes in current-state.md: "Vision updated: added Notification entity (discovered during Feature 15). No re-plan needed — Notification entity is self-contained."
        </approach>
        <expected_outcome>Vision stays fluid. Minor drift resolved by PD editing vision docs directly.</expected_outcome>
      </step>

      <step number="3">
        <title>Phase 4 Re-evaluation: Major Drift After Feature 22</title>
        <approach>
          Feature 22 (Supplier Management) reveals that the vision's permission model is fundamentally wrong. The vision described "simple owner/staff roles" but multi-location restaurants need per-location permissions. This affects Features 23-28 (all location-based features).

          PD flags for user: "The vision's permission model conflicts with what we need for multi-location. Features 23-28 are affected." User selects "Re-plan remaining features." PD spawns Architect with updated vision docs (PD edited permissions section) + list of completed features (1-22). Architect revises roadmap for Features 23-35, adding a new "Per-Location RBAC" feature and adjusting dependent features. PD updates total_features in state.json from 35 to 36.
        </approach>
        <expected_outcome>Vision re-evaluation caught a fundamental issue before it cascaded into 6 features of wrong code.</expected_outcome>
      </step>
    </workflow>

    <completion>
      After all 36 features complete, state.json has status="complete". All vision docs reflect the true built state of the app. The initial 28-feature vision evolved to 36 roadmap features through the re-evaluation process, but every change was intentional and documented.
    </completion>

    <key_takeaways>
      <takeaway>Vision re-evaluation after every feature is critical — it catches drift before it compounds.</takeaway>
      <takeaway>Minor drift (new entity, small UI change) → PD edits vision directly. No re-plan.</takeaway>
      <takeaway>Major drift (affects 3+ remaining features) → re-spawn Architect for remaining features only.</takeaway>
      <takeaway>The vision and roadmap are living documents that evolve with the build.</takeaway>
      <takeaway>Waves prevent spaghetti: Feature 15 added a Notification entity without touching Feature 1's data model code — it just extended it.</takeaway>
    </key_takeaways>
  </example>

  <example name="resume_after_interruption">
    <scenario>
      The context window was cleared mid-project. The PD resumes by reading state.json.
    </scenario>

    <workflow>
      <step number="1">
        <title>State Re-hydration</title>
        <approach>
          Read state.json: current_phase=3, status="in_progress", total_features=36, current_feature_index=12. PD announces: "Resuming. 12/36 features complete. Proceeding to Feature 13." Proceeds directly to Phase 3 Step 1.
        </approach>
      </step>
    </workflow>

    <key_takeaways>
      <takeaway>state.json enables deterministic resume — no markdown parsing or guesswork required.</takeaway>
      <takeaway>The PD should never re-initialize state.json if it already exists and is valid.</takeaway>
    </key_takeaways>
  </example>

  <example name="handling_test_failure">
    <scenario>
      After Feature 18 (Payment Abstraction + Stripe) completes, the regression test suite fails.
    </scenario>

    <workflow>
      <step number="1">
        <title>Phase 4: Test Failure Detected</title>
        <approach>
          Run test suite. 3 failures in checkout module. PD spawns code subtask with test_fix_prompt_pattern. Code subtask finds: Feature 18 changed the payment interface signature but didn't update checkout tests. Fixes tests. PD re-runs — all pass.
        </approach>
      </step>
    </workflow>

    <key_takeaways>
      <takeaway>Never proceed to audit or PR with failing tests.</takeaway>
      <takeaway>If the code fix fails after one retry, escalate to the user.</takeaway>
    </key_takeaways>
  </example>

  <example name="ad_hoc_request_already_covered">
    <scenario>
      Mid-project, the user makes an ad-hoc request. PD triages it as "already covered by the next feature" — this is Path A (Simple), no pipeline needed.
    </scenario>

    <user_request>Actually, before the next feature — can you fix the date formatting in the invoice table? It's showing UTC timestamps instead of the user's local timezone.</user_request>

    <workflow>
      <step number="1">
        <title>Phase 3 Step 0: Triage — Read Source Code</title>
        <approach>
          PD reads state.json: current_phase=3, current_feature_index=8, total_features=35. Request doesn't match resume patterns. PD reads the invoice table component to understand the current date formatting implementation.
        </approach>
      </step>

      <step number="2">
        <title>Phase 3 Step 0: Triage — Read Roadmap & Vision</title>
        <approach>
          PD reads roadmap.md. Feature 9 is "Invoice Management UI" (immediate next feature) with description "Display dates in user's local timezone." Vision docs confirm this is a core requirement. The request is already covered by the immediate next feature.

          PD answers the triage question: this doesn't alter what the app IS — it's an implementation detail already planned. Path A.

          PD tries to convince user to wait: "Feature 9 (Invoice Management UI) is literally the next feature and explicitly covers date formatting in user timezone. Building it now solves your request as part of a complete feature rather than as a one-off fix."

          User agrees: "Build Feature 9 now."
        </approach>
      </step>

      <step number="3">
        <title>Phase 3 Step 1: Spawn FD for Feature 9</title>
        <approach>
          Feature 9 is already next. No reordering. PD proceeds to normal FD spawn flow. The FD Architect derives acceptance criteria from vision docs including date formatting.
        </approach>
      </step>
    </workflow>

    <completion>
      The ad-hoc request was classified as Path A (already covered by the immediate next feature). PD convinced user to wait for Feature 9 rather than doing a premature one-off fix. No pipeline needed — just normal FD flow.
    </completion>

    <key_takeaways>
      <takeaway>PD reads source code before classifying — understanding the current implementation enables informed advice.</takeaway>
      <takeaway>PD tries to convince the user to wait when a request is already covered — premature fixes cause rework.</takeaway>
      <takeaway>When the covering feature IS the next feature, no pipeline is needed — just continue with normal FD flow.</takeaway>
    </key_takeaways>
  </example>

  <example name="ad_hoc_request_path_a_simple">
    <scenario>
      The user requests a small, standalone fix. PD triages as Path A (Simple Change) — no vision alteration.
    </scenario>

    <user_request>I noticed the sidebar collapses when you resize the window below 900px, but it should stay open until 768px. Can you fix that?</user_request>

    <workflow>
      <step number="1">
        <title>Phase 3 Step 0: Triage</title>
        <approach>
          PD reads the sidebar component CSS. Checks roadmap.md: no upcoming feature covers responsive breakpoints. Checks vision docs: no breakpoint specifications at this level. The triage question: does this alter what the app IS? No — it's a CSS behavior refinement within the existing vision. Path A.

          PD recommends Path A. User agrees.
        </approach>
      </step>

      <step number="2">
        <title>Path A: Execute Code Subtask</title>
        <approach>
          PD spawns Code subtask via ad_hoc_code_subtask_prompt. One CSS file changed. No architectural impact. Tests pass.
        </approach>
      </step>

      <step number="3">
        <title>Path A: PD Sanity Check</title>
        <approach>
          Single-file change → PD reviews the changed file + its direct dependencies (source code review scaled to magnitude), plus ALL vision files, ALL planning files, and roadmap (always comprehensive). Vision docs don't specify breakpoints at this granularity and no other vision/planning file contradicts the change. Check passes.

          PD documents in current-state.md: "Ad-hoc: Fixed sidebar collapse breakpoint (900px → 768px)." Commits. Continues with next feature.
        </approach>
      </step>
    </workflow>

    <completion>
      Path A handled a small CSS fix with minimal overhead: one Code subtask, PD sanity check with comprehensive vision/planning review, one dashboard update.
    </completion>

    <key_takeaways>
      <takeaway>Path A is for changes that refine implementation without altering what the app IS.</takeaway>
      <takeaway>PD sanity check always reads ALL vision files, ALL planning files, and roadmap — regardless of change magnitude. Source code review still scales proportionally.</takeaway>
      <takeaway>If Code reports ARCHITECTURAL IMPACT, PD escalates to Path B — the change was bigger than it seemed.</takeaway>
    </key_takeaways>
  </example>

  <example name="ad_hoc_request_path_b_vision_realignment">
    <scenario>
      Mid-project, the user wants to change a core interaction pattern described in vision docs. PD triages as Path B (Vision Realignment Pipeline) and runs the full 7-step pipeline.
    </scenario>

    <user_request>I don't like our plan for authentication. We currently have email/password in the vision, but I want to switch to OAuth-only with Google and GitHub. No email/password at all.</user_request>

    <workflow>
      <step number="1">
        <title>Phase 3 Step 0: Triage — Path B</title>
        <approach>
          PD reads the current auth code (already built in Feature 2 — email/password with JWT). Checks roadmap: Feature 14 is "User Settings & Preferences" which builds on the email/password auth model. Feature 18 is "Team Invites" which sends email invites.

          PD answers the triage question: this ALTERS WHAT THE APP IS. It changes a core interaction pattern (how users sign in), removes a planned capability (email/password), and affects 3+ remaining features (F14, F18, and the Realignment needed for F2's already-built auth). Path B.

          PD discusses: "Switching to OAuth-only means: (1) Feature 2's email/password auth needs realignment, (2) Feature 14's user settings need redesign (no password change flow), (3) Feature 18's team invites need a different mechanism (OAuth-based instead of email). We should also consider: adding a profile picture from OAuth providers, handling account linking (Google + GitHub → same user), and what happens if a provider is down."

          User confirms: OAuth-only, Google + GitHub, account linking via email matching, no fallback auth.
        </approach>
      </step>

      <step number="2">
        <title>Path B Step B2: Aggregate & Present</title>
        <approach>
          PD aggregates changes: Auth: OAuth-only (Google, GitHub), remove email/password. Account linking: match by verified email. Affected features: F2 (realign), F14 (redesign user settings), F18 (redesign team invites). Realignment Feature likely needed. User approves.
        </approach>
      </step>

      <step number="3">
        <title>Path B Steps B3-B4: VH Update + Critic</title>
        <approach>
          VH interviews user about auth change, updates all vision files (app-overview.md, feature-inventory.md, features/authentication.md, screens/login.md + login.html mock, data/user.md). Reports delta: screens/login.html mock changed (now shows OAuth buttons), data/user.md added provider_linked_accounts entity.

          VH Critic finds: features/team-invites.md still references "send invitation email to user's email" but data/user.md no longer stores unverified email separately. Fixes the contradiction.
        </approach>
      </step>

      <step number="5">
        <title>Path B Steps B5-B6: Architect Update + Critic</title>
        <approach>
          Architect reads all files. Analyzes knowledge/ files: Feature 2 built email/password auth with JWT. Gap: existing code doesn't support OAuth. Inserts Realignment Feature as F9: "Realignment — OAuth-Only Authentication."

          Updates roadmap: F14 becomes "User Profile & Linked Accounts," F18 becomes "OAuth-Based Team Invites." Updates planning files: abstractions.md (new OAuth provider interface), data-model.md (provider_linked_accounts entity), integration-points.md (Google OAuth, GitHub OAuth). Flags 2 existing E2E tests invalidated (login-flow.spec.ts, password-reset.spec.ts).

          Architect Critic finds: abstractions.md defines OAuth provider interface but doesn't specify token refresh strategy. Architect fixes. Critic approves.
        </approach>
      </step>

      <step number="7">
        <title>Path B Step B7: PD Final Review</title>
        <approach>
          PD reviews all changed files via git diff. Verifies: roadmap forward-dependencies hold (F14 depends on F9 Realignment, F18 depends on F14), total_features updated (35→36), no orphaned references, Realignment Feature properly positioned. Sets realignment_step=null. Proceeds to Phase 3 Step 1 — next feature is F9 (Realignment).
        </approach>
      </step>
    </workflow>

    <completion>
      A vision-altering mid-project change (email/password → OAuth-only auth) triggered the full 7-step Path B pipeline. VH updated vision files and mocks. VH Critic fixed contradictions. Architect inserted a Realignment Feature (F9) to retrofit existing code, updated roadmap (3 features changed), planning files, and knowledge files. Architect Critic caught missing token refresh strategy. PD's final review confirmed alignment. Total: 4 spawns (VH, VH Critic, Architect, Architect Critic) + PD review.
    </completion>

    <key_takeaways>
      <takeaway>Path B is for changes that alter what the app IS — not every mid-project request needs this pipeline.</takeaway>
      <takeaway>The VH delta report is critical — it catches divergences between what was discussed and what the user actually decided during the interview.</takeaway>
      <takeaway>Realignment Features retrofit existing code BEFORE new features build on the corrected foundation.</takeaway>
      <takeaway>The abort hatch is always available — user can revert to original roadmap at any step.</takeaway>
    </key_takeaways>
  </example>

  <example name="architectural_drift_detection">
    <scenario>
      Feature 24 introduces direct DB queries in UI components, bypassing the repository pattern from Wave 1.
    </scenario>

    <workflow>
      <step number="1">
        <title>Phase 4: Drift Detected in Audit</title>
        <approach>
          PD reads knowledge files. architecture.md describes repository pattern. Feature 24's code uses direct queries in components. PD flags: "Architectural Drift — Moderate. Feature 24 bypasses Wave 1 repository abstraction." Health: Fair. At PR/merge gate, presents finding. User injects refactoring feature. PD inserts "Feature 25: Refactoring — Extract Data Access to Repository Layer" in Wave 4.
        </approach>
      </step>
    </workflow>

    <key_takeaways>
      <takeaway>Wave 1 abstractions must be enforced. Drift is caught at audit, not at project end.</takeaway>
      <takeaway>Health rating must be evidence-based. Cite specific files and patterns.</takeaway>
    </key_takeaways>
  </example>
</complete_examples>